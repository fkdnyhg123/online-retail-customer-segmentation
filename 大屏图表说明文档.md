# 在线零售客户分群分析 — 大屏图表说明文档

> 课程: 大数据分析应用综合实践
> 数据集: UCI Online Retail II（英国在线零售商，2009-12-01 ~ 2010-12-09，原始 525,461 行 × 8 字段）
> 技术栈: Streamlit + Plotly + scikit-learn + mlxtend + networkx + pandas
> 在线地址: https://online-retail-customer-segmentation-eswdabwlnoaqev3rzvrhxq.streamlit.app/

---

## 怎么读这份文档

1. **条目标题 = 屏幕上那行小标题**（连 emoji 一起照抄），顺序也和页面从上到下完全一致，答辩时可以逐条对照着讲。只有 3 个条目标了"屏幕无小标题"（页面上那里确实是裸的表格/卡片，没有标题行），其余都能在屏幕上原样找到。
2. 每个条目最多四行：**屏幕**（看到什么）/ **代码**（绘图在哪、计算在哪）/ **算法**（怎么算出来的）/ **结论**（一句话说明什么）。没有算法可讲的条目会省掉「算法」那一行；只有会被追问的地方才多一行 **⚠️ 口径**。
3. 行号基于 commit `f8cded1`。**行号会随改动漂移，找不到就直接搜条目标题里的字符串或括号里的函数名**，比行号可靠。

### 找代码总表（6 个页面）

| 侧边栏导航项 | 页面大标题 | 代码区间 | 主要计算函数所在文件 |
|:---|:---|:---|:---|
| 📈 数据概览 | 同左 | `app.py:559-776` | `utils/data_cleaner.py` |
| 🔍 数据探索 | 同左 | `app.py:781-887` | `app.py` 内的 `compute_*` 函数 |
| 👥 客户特征分析 | 同左 | `app.py:892-1124` | `utils/rfm.py` |
| 🎯 K-Means 聚类 | 🎯 K-Means 客户分群分析 | `app.py:1129-1516` | `utils/clustering.py` |
| 🔗 关联规则分析 | 🔗 关联规则分析 (Market Basket Analysis) | `app.py:1521-1906` | `utils/association.py` |
| 📦 商品与国家调查 | 同左 | `app.py:1911-2075` | `utils/product_country.py` |

导航面板与侧边栏数据集信息: `app.py:543-554`。页面 4/5 的参数控件挂在侧边栏，切页时自动出现/消失（`app.py:1133-1158`、`app.py:1526-1541`）。

**数据使用范围**: 只有页面 2 提供时间筛选器，且筛选**只影响本页**；RFM、聚类、关联规则、商品与国家四块始终用完整清洗后数据（399,599 行 / 4,295 客户）。

---

## 数据从哪来

- **入口**: `load_and_clean()` @ `app.py:248-291`，全站在 `app.py:294` 调用一次，六个页面共用返回值；优先读仓库里的 `.cache_raw.parquet` / `.cache_cleaned.parquet`，没有才解析 43MB 的 xlsx 并回写（`.cache_meta.json` 存数据指纹用于判断该不该重建）。
- **链路**: 原始 `raw_df`（525,461 行 × 8 列，UCI Online Retail II）→ `utils/data_cleaner.py:49` `clean_data()` 七步清洗 → `cleaned_df`（399,599 行）→ 每行算 `Revenue = Quantity × Price`。
- **客户级表 `rfm_df` 的组装**: `calculate_rfm()` + `calculate_extended_features()` merge + `add_rfm_scores()`（`app.py:285-290`），即页面 3、页面 4 的共同输入。

---

# 页面 1 · 📈 数据概览

### 📋 数据理解与基础探索
- **屏幕**: 三列卡片——原始记录数、时间跨度（宽幅自定义卡片）、覆盖国家。
- **代码**: `app.py:564-579`。
- **算法**: `len(raw_df)`、`raw_df['Country'].nunique()`；日期区间来自 `cleaning_summary`。
- **结论**: 525,461 行、跨 40 个国家，英国为绝对主力市场（占 90% 以上的行）。时间跨度用宽卡片是为了让日期不被截断。

### 🔎 原始数据质量报告
- **屏幕**: 左列 6 行「问题名 + 条数 + 占比」，右一张环形饼图（hole=0.45）。
- **代码**: `app.py:592-608`（问题名清单来自常量 `QUALITY_ITEM_KEYS` @ `app.py:195`）· 计算 `utils/data_cleaner.py:31` `get_data_quality_report()`。
- **算法**: 每类一条布尔条件计数 `df[条件].shape[0]`，占比 = 条数 ÷ 525,461 × 100。
- **结论**: 缺失客户 ID 最多（107,927 条 / 20.5%，游客下单未注册），取消订单 10,206 条 / 1.94%，负数量 12,326、零负价格 3,690、精确重复 6,865、非商品编码 2,769。这些不清掉，RFM 与聚类全部失真。
- ⚠️ **口径（易被追问）**: 6 类**互不互斥**，同一行可以既是取消单又是负数量。饼图的分母是 6 类合计 **143,783 次「问题命中」**（扇区之和=100%），左侧文字的分母是 **525,461 行**——两处口径不同，文字百分比相加当然不等于 100%，这不是算错。

### 🧹 数据清洗步骤说明
- **屏幕**: 一张 7 步流程表 + 下方 4 个指标卡（清洗前 / 清洗后 / 移除 / 移除比例）。
- **代码**: `app.py:613-637` · 计算 `utils/data_cleaner.py:49` `clean_data()`。
- **算法（7 步按顺序执行，一行代码一步）**:
  1. 去取消订单 `data_cleaner.py:75`（Invoice 以 `C` 开头）
  2. 去非商品编码 `:78`（POST / BANK CHARGES / C2 / D / DOT / M / PADS / CRUK，常量在 `:22`）
  3. 去缺失描述 `:81` 4. 去负数量 `:84` 5. 去零/负价格 `:87` 6. 去精确重复 `:90` 7. 去缺失客户 ID `:94`
  最后 `:101` 生成 `Revenue = Quantity × Price`。
- **结论**: 先业务问题（取消单、非商品）后数据问题（缺失、异常、重复），每步都有业务理由——负数量是退货，不该进正向销售分析。规模变化 525,461 → 399,599，移除 125,862 行（**23.95%**）。

### 📊 清洗前后数据对比
- **屏幕**: 上分组柱状图（4 项规模指标，清洗前红/清洗后绿，柱顶标数值），下 7 行对比表（再加平均数量、平均单价、时间范围）。注意屏幕上这行小标题是页面大标题下的**四级标题**——代码用 `st.markdown("#### 📊 清洗前后数据对比")` 写的，不是 `st.subheader`，搜函数名搜不到时按这串文字搜。
- **代码**: `app.py:640-705`（图 `:675`，表 `:699`，口径 caption `:700-705`）· 汇总 `utils/data_cleaner.py:107` `get_cleaning_summary()`。
- **结论**: 记录 -24%，发票数降幅最大（28,816 → 19,011，-34%，很多无效发票整张消失），但**客户只少 88 人（4,383 → 4,295）**——清掉的是无效交易，不是真实客户。商品 4,632 → 4,011。
- ⚠️ **口径**: 「清洗前平均数量 10.3」用全部行；「清洗前平均单价 $4.98」只统计 `Price > 0` 的行。**同一张表两行口径不同**（全行单价是 $4.69），页面已用 caption 标出。
- **追问「平均数量为什么反而 +3.5（10.3 → 13.8）」**: 这是加权平均的算术必然——删掉一批均值低于整体的行，剩下的均值一定上升。按清洗步骤**互斥归因**（一行只算真正删掉它的那步）:

  | 步骤 | 移除行数 | 该批平均数量 | 抬升整体均值 |
  |:---|---:|---:|---:|
  | 1 去取消订单 | 10,206 | **-20.9 件** | +0.619 |
  | 2 去非商品编码 | 2,227 | +3.9 件 | +0.031 |
  | 3 去缺失描述 | 2,922 | -22.1 件 | +0.189 |
  | 4 去负数量 | 294 | **-523.8 件** | +0.309 |
  | 5 去零/负价格 | 464 | +63.7 件 | **-0.048** |
  | 6 去精确重复 | 6,830 | +2.8 件 | +0.118 |
  | 7 去缺失客户 ID | 102,919 | +2.84 件 | **+2.245** |
  | **合计** | **125,862** | **-0.66 件** | **+3.463** |

  增量的三分之二来自第 7 步（10.3 万件游客小额单，平均只 2.84 件）。第 4 步删的行最极端（-523.8 件）但只有 294 行，权重不够；第 5 步是唯一**拉低**均值的一步。
  **两张数为什么核对不上**: 上面的质量报告按「问题类型」计数（143,783，可重叠），本表按「清洗步骤」归因（125,862，互斥），各自自洽但不能互相核对。
  **平均单价反向下降（$4.98 → $3.01）是同一机制**: 被删那批行整体平均单价高达 **$10.02**（含 POST 邮费、BANK CHARGES 这类高价非商品行）。一句话答辩: 清洗前后**不是同一个总体**，`+3.5` 说的是样本构成变了，不是数据变好了。

### ✅ 清洗后数据质量报告
- **屏幕**: 三列 7 行复检表（检查项 / 清洗前命中 / 清洗后命中）+ 3 个指标卡（客户数 4,295、商品数 4,011、记录数 399,599）。
- **代码**: `app.py:710-728`（表 `:718`）· 计算 `utils/data_cleaner.py:31`，在 `app.py:283` 用**同一个函数**对清洗后数据再跑一遍。
- **结论**: 7 项在清洗后的 399,599 行上**全部为 0**。这是"双重检查"——不写"已无质量问题"这种口头结论，而是把前后命中数并排摆出来自己看，包括最小的两类（非商品编码 0.53%、缺失描述 0.56%）也确实归零。
- 复检表比饼图多出「缺失描述」一项（`QUALITY_EXTRA_KEYS` @ `app.py:204`），所以是 7 行；饼图保持 6 类口径。

### 📊 清洗后核心指标
- **屏幕**: 一行 5 张卡片。
- **代码**: `app.py:733-747` · 计算 `utils/rfm.py:71` `get_rfm_stats()`（总收入）。
- **结论**: 交易 399,599 行 / 客户 4,295 / 商品 4,011 / 总收入 **$8,644,153** / 平均订单 **$454.69**（= 按发票汇总收入后取均值，`app.py:744`）。

### 📈 月度收入趋势
- **屏幕**: 面积图，2009-12 至 2010-12 逐月总收入。
- **代码**: `app.py:752-759` · `app.py:753` `resample('ME')['Revenue'].sum()`。
- **结论（实测，万元）**: 2009-12 67.80 → 1 月 53.73 → **2 月 49.79（全年低点）** → 3 月 66.60 → 4-8 月在 58~63 之间走平 → 9 月 **80.55** → 10 月 **101.16** → **11 月 115.60（全年最高）**。读法是上半年平台、9 月起陡峭冲高（圣诞采购季）；1-6 月合计 350.9 万，7-11 月五个月已 414.9 万。
- ⚠️ **口径**: **12 月的柱子全图最低（约 30.8 万）不是业务下滑**，数据只到 2010-12-09，那只是 9 天的收入。

### 🌍 国家收入排名 Top 10
- **屏幕**: 横向柱状图，前 10 国收入。
- **代码**: `app.py:762-776` · `groupby('Country')['Revenue'].sum().sort_values().head(10)`（`app.py:763`）。
- **结论**: 英国 **84.75%** 绝对主导，其后 **爱尔兰(EIRE) 3.88% > 荷兰 3.08% > 德国 2.12%**。地域高度集中 → 这是一家主要做英国本土生意的在线零售商；海外份额虽小，但用法见页面 6 的「本土 vs 海外」二分对比。

---

# 页面 2 · 🔍 数据探索

### 📅 时间范围筛选 (仅影响本页图表)
- **屏幕**: 一个默认收起的 expander，内含起始月/结束月两个下拉。
- **代码**: `app.py:785-808` · 筛选函数 `filter_explore_data()` @ `app.py:298`。
- **结论**: 这是全站的图表联动交互，结果只作用在本页的分段统计图和热销排名。**为什么不给 RFM/聚类也加时间筛选**: RFM 衡量的是客户全生命周期，截一段会让大量活跃客户被误判成"新客户"。

### 📊 字段值分布
- **屏幕**: 三个 Tab（📦 数量 / 💲 单价 / 💰 收入），每个 Tab = 4 张统计卡（中位数、均值、中间 50% 范围、前 10% 阈值）+ 一张分段柱状图（柱顶标占比%）+ 一行分位数脚注。
- **代码**: `app.py:811-873`（图 `:865`）· 计算 `compute_distribution_stats()` @ `app.py:304`。
- **算法**: 按业务阈值切区间后 `value_counts` 分段计数，颜色由浅到深映射条数。
- **为什么用分段柱状图不用直方图**: 早期是 80 箱学术直方图，极端离群值把 X 轴拉很长、主体分布挤在最左看不清，而且评委更习惯"业务分段 + 占比"的读法。
- ⚠️ **口径**: Y 轴是**交易行数**（一行 = 发票里的一个商品明细行），不是发票张数。
- **数量**: 中位数 5 件、均值 13.8 件（差距全靠批发大单拉开）。实测占比 **6-12 件 33.7% > 1-2 件 33.5% > 3-5 件 16.9% > 13-24 件 8.2% > 25-50 件 4.8% > 50+ 件 2.9%**——最大档不是最小那档，别顺着"越小越多"讲；业务读法是"零散小单 + 成批购买"两类客户混在一张图上（它是批发型零售）。
- **单价**: $0-1 20.9% + $1-2 31.4% + $2-5 33.1% + $5-10 11.7% = **97.1%**，中位数 $1.95、上四分位 $3.75，最右档 $20+ 只有 0.2%。低价日用品为主，符合"在线杂货/礼品店"模式。
- **收入**: = 数量 × 单价，**不是逐段递减**，最高峰在中间偏左的 **$10-20 档（35.8%）**，其后 $0-5 25.5%、$5-10 17.1%、$20-50 14.5%、$50-100 4.2%、$100+ 2.9%；$0-20 三档合计 78.4%。中位数 $12.5、均值 **$21.6**（= 864.4 万 ÷ 399,599 行）。这种右偏正是后续 RFM 要做排名/组合变换的原因。

### 🏆 热销商品排名 (按收入)
- **屏幕**: 横向柱状图 + 页面内 Top-N 滑块（5-30，默认 20，图高随 N 变）。
- **代码**: `app.py:877-886` · 计算 `compute_top_products()` @ `app.py:337`。
- **算法**: `groupby(['StockCode','Description'])` 聚合收入/销量/发票数，按收入降序取前 N。
- **结论**: 头部是家居装饰与节日用品（第一名 WHITE HANGING HEART T-LIGHT HOLDER 白色心形烛台架），收入远超后面——典型二八定律。配合时间筛选器可以对比不同季度的爆款差异。

---

# 页面 3 · 👥 客户特征分析

> 页面定位（屏幕顶部就写着，`app.py:894`）: 在 R/F/M 之上再加**两个非 RFM 客户特征**（AOV、品类广度），5 个维度共同构成页面 4 聚类的输入。

### ① RFM 核心指标紧凑表（屏幕无小标题，该页第一张表）
- **屏幕**: 3 行 5 列表格，行标签 `**R** · 最近购买间隔` / `**F** · 购买频率` / `**M** · 消费金额`。
- **代码**: `app.py:897-909` · 计算 `utils/rfm.py:9` `calculate_rfm()` + `compute_rfm_segments()` @ `app.py:348`。
- **算法**: 按 Customer ID 分组——R =（快照日 - 最后购买日）天数，快照日 = max(InvoiceDate)+1；F = 不重复发票数；M = 该客户总收入。
- **结论**: R 中位数约 52 天。F 均值远大于中位数、M 差得更多 → 少量高频高额客户拉高均值，这就是必须处理的重偏态（M 偏度 ~24）。
- 设计说明: 早期用 6 张独立指标卡，占地方又不醒目，改成三行对齐的紧凑表；统计量刻意用"上四分位""前 10% 阈值"这种中文，不用 P75/P90。

### ② R/F/M 分布分段统计图（屏幕无小标题，三张并排）
- **屏幕**: 三张与页面 2 同款的分段柱状图（Y 轴=客户数，柱顶标占比%），下方一行解读。
- **代码**: `app.py:913-945`。
- **结论**: R 相对均匀、轻微右偏，200 天以上基本可判流失；F 严重右偏（绝大多数只有 1-5 次）；M 三个维度里偏态最重——**这就是对数变换不够用、排名/组合变换更有效的原因**。

### 🧩 扩展客户特征 (非 RFM)
- **屏幕**: 两张分段统计图（AOV、品类广度）+ 一张业务含义表（计算方式/含义/中位数/上四分位）+ 一段随数据动态生成的解读。
- **代码**: `app.py:949-991`（图 `:974`）· 计算 `utils/rfm.py:102` `calculate_extended_features()`，列名与文案来自 `EXTENDED_FEATURE_INFO` @ `utils/rfm.py:86`，分段 `compute_extended_segments()` @ `app.py:466`。
- **算法**: AOV = 客户总收入 ÷ 发票数；品类广度 = 不重复 StockCode 数。
- **为什么是这两个（答辩重点）—— 7 个候选全算过，按「与 R/F/M 弱相关 + 分布可控」筛**:

  | 候选 | 与 R | 与 F | 与 M | 偏度 | 零值% | 结论 |
  |:---|:---:|:---:|:---:|:---:|:---:|:---|
  | **AOV 平均客单价** | -0.05 | 0.09 | 0.33 | 11.6 | 0 | ✅ 候选里与 R/F/M 最不相关 |
  | **品类广度** | -0.29 | 0.66 | 0.41 | 6.2 | 0 | ✅ 与 F 中等相关但语义正交（F 是"来几次"，广度是"每次买多宽"） |
  | 活跃月数 | -0.43 | 0.70 | 0.37 | 1.7 | 0 | ❌ 与 F 相关 0.70，信息重复 |
  | 退货次数 | -0.18 | 0.73 | 0.46 | 9.9 | 60.1 | ❌ 与 F 高相关 + 六成客户为 0 |
  | 生命周期跨度 | -0.50 | 0.47 | 0.23 | 0.4 | 34.6 | ❌ 三分之一客户只买过一次，跨度为 0 |
  | 单次平均件数 | 0.00 | 0.01 | 0.10 | 45.7 | 0 | ❌ 最大 87,167 件/单，极端重尾 |
  | 退货率 | 0.01 | 0.03 | 0.03 | 2.9 | 60.1 | ❌ 定义失真（退货发票数可超过正向发票数） |

  入选的两个特征彼此相关仅 **0.16**。**加特征的门槛**: 若与 F 相关 0.7 以上，塞进聚类只是把 F 重复算一遍，分群不会变好。
- ⚠️ **口径（两行「退货」）**: 退货行在清洗第 1 步就被删了，只能用**原始数据**回算。可复现的是**发票级**口径: 退货次数与 R/F/M 相关 -0.16/0.72/0.44、偏度 8.16、零值 60.1%；退货率 0.42/-0.29/-0.13、偏度 2.49。上表那两个数字出自当年另一版按退货行数计数的口径（偏度 9.9）。**两套口径结论一致**（与 F 高相关 + 六成零值 / 分子可大于分母），不影响任何采用决策; 被追问数值时引用发票级这组。

### 🔗 五维特征相关性
- **屏幕**: 左文字右热力图（列宽 1:2），5×5 Pearson 矩阵，RdBu_r 色阶，格内标 `.2f`。
- **代码**: `app.py:993-1027`（图 `:1027`）· `rfm_df[[5列]].corr()` @ `app.py:997`。
- **结论**: 一图撑两个结论——① F 与 M 中高度正相关（r≈0.65），这是聚类里把 F/M 合并成"参与度"的理论依据；② 新特征与 R/F/M 基本独立（AOV–M≈0.33、品类广度–F≈0.66、AOV–品类广度≈0.16），这正是选它们的理由。R 与 F/M 相关低（R–F≈-0.25、R–M≈-0.12），说明时效性是独立维度。
- 页面文案里的系数是从 `corr` 实时取值的（`app.py:998-1002`），不写死，换数据不会脱节。

### 🔗 RFM 关系气泡图（两张气泡图）
- **屏幕**: 左「频率 vs 最近购买」（气泡大小/颜色=消费金额，Viridis），右「频率 vs 消费金额」（气泡=最近购买，RdYlGn_r：绿=近期活跃）。
- **代码**: `app.py:1030-1050`，每个点 = 一位客户，直接用原始 R/F/M。
- **结论**: 高频客户更近期、消费更高（右侧大气泡偏黄）；左上=低频已流失，右下=核心 VIP；左下大量小点是一次性"路人"客户。右图 F–M 正相关 r≈0.65，但右上角极端客户的金额远超其余，再次印证偏态。

### 📊 RFM 评分分群 (分位数法)
- **屏幕**: 一张各分群客户数柱状图（Blues 色阶，柱上标人数）+ 收起的「📋 查看分类依据」expander（内含评分标准表与分群规则表）。
- **代码**: `app.py:1054-1126` · 评分 `utils/rfm.py:37` `add_rfm_scores()`，规则 `assign_segment()` @ `app.py:1057`，expander `app.py:1087`。
- **算法**: 三维各按分位数分 1-5 档（5 最优），**统一先 `rank(method='first')` 再等频分箱**，所以每档人数严格均匀（4,295 → 约 859 人/档）；R 反转（越近期分越高）。组合规则: R≥4&F≥4&M≥4→冠军客户；R≥3&F≥3→忠诚；R≥4&F≤2→新客；R≤2&F≥3&M≥3→流失风险；R≤2&F≤2&M≤2→流失；其余→潜力/沉睡/待开发。
- **为什么 R 也必须先 rank**: Recency 是整数天数、重复值密集，直接 `qcut` 会让箱边界落在重复值上并被 `duplicates='drop'` 合并，档位不均、下游阈值跟着偏。
- **结论**: 人数最多的是**忠诚客户 972 人（22.6%）与冠军客户 908 人（21.1%）**；冠军客户以 21.1% 的人贡献 **64.4%** 收入。这是"规则分群"，与页面 4 的机器学习分群形成对照: 规则法可解释性强，但阈值是人定的、缺乏数据驱动。（页面这段解读由 `seg_counts` 与 `scored['Monetary']` 动态计算，改口径不会出现"图上 A、文字 B"。）
- 细节: expander 里的表格刻意用连字符写区间（"1-5 档"）而非波浪号——同一 Markdown 块里两个 `~` 会被解析成删除线把文字划掉。

---

# 页面 4 · 🎯 K-Means 聚类（屏幕大标题「🎯 K-Means 客户分群分析」）

### ⚙️ 聚类参数（侧边栏，屏幕标题就是这行）
- **屏幕/代码**: `app.py:1133-1158`。

  | 控件 | 默认 | 行号 | 作用 |
  |:---|:---|:---|:---|
  | K 搜索范围（起始/结束） | 2 / 10 | `:1139` `:1142` | 肘部与轮廓扫描的区间；结束值下限会跟随起始值抬升，避免空区间或"值低于下限"报错 |
  | Winsorize 截断（上分位） | 0.995 | `:1145` | 把 F/M 最大的 0.5% 截断，防止极端客户把质心拉偏 |
  | 聚类数量 K | **4** | `:1148` | 上限 15，与调色板色数对齐；默认 4 = 页面「第一步」轮廓系数的推荐值 |
  | 输入特征集 | RFM + 扩展特征 | `:1152` | 两档；**每档自动配对实测最优的变换方法**，所以下拉标签里直接写出方法，侧边栏不再单独暴露"方法"选项 |

- **Winsorize 是什么**: 将超过指定分位的极端值截断到该分位值（这里截 F/M 的 top 0.5%）。不截的话，买 500 次/消费 $20,000+ 的少数客户会主导欧氏距离。
- **关于「输入特征集」两档**: `RFM + 扩展特征`（5 维）是默认，用来满足"除 RFM 外至少 2 个特征"；`仅 RFM`（3 维）保留做**对照实验**，用来证明新特征确实参与了分群。

### 📐 第一步: 确定最优聚类数 K
- **屏幕**: 左肘部法则折线（K vs WCSS），右轮廓系数折线（⭐ 标推荐 K）+ 一条 `st.info` 的 K 值选择依据（`app.py:1213`）。
- **代码**: `app.py:1166-1216`（图 `:1181`、`:1210`）· 计算 `cached_prepare_and_elbow()` @ `app.py:423` → `utils/clustering.py:33` `prepare_features()` + `utils/clustering.py:149` `find_optimal_k()`。
- **算法**: 对 K=2..10 各训练一次 K-Means（`n_init=10, max_iter=500, random_state=42`），记录 `inertia_`（WCSS）与 `silhouette_score`。⭐ 的位置在 **K≥3** 里取最高（`app.py:1192-1205`）。
- **结论**: WCSS 随 K 下降，拐点在 4-5；轮廓系数**通常在 K=2 最高**，所以刻意跳过 K=2。默认档（RFM+扩展→PCA）: K=2 0.459 / K=3 0.384 / **K=4 0.397** / K=5 0.383 / K=6 0.377；对照档（仅 RFM→组合特征）: K=2 0.509 / K=3 0.445 / **K=4 0.449** / K=5 0.420 / K=6 0.428。**两档绝对值不可横向比**（维度不同）。
- **为什么 K=2 最高却不是它**: K=2 只是把客户粗分"活跃/沉睡"两团，分离度最大，反映数据的双峰结构；对精细化营销（VIP/新客/流失差异化触达）粒度不够，所以推荐值限制在 K≥3。
- **K=4 的两层理由**: 统计上 K≥3 里最高（默认档 0.3967，K=3 0.3844、K=5 0.3825），肘部也落在 4-5；业务上正好分出 VIP/新客/流失/沉睡四类。**不做经典五类**是因为 K=5 只会把中间态客户切成两个边界模糊的簇（0.3825 < 0.3967），少一个簇换来更清晰的边界。滑块随时能调到 5 现场验证。

### 🔬 两种特征变换方法 — 组合特征 vs PCA (点击展开)
- **屏幕**: 收起的 expander（`app.py:1218`），内含方法对比表、配对依据表、共用流程说明和一张**随侧边栏实时刷新**的对比表（`app.py:1262`）。
- **代码**: `app.py:1218-1263` · 计算 `cached_compare_methods()` @ `app.py:440`。
- **四种方法都实测过（K=4、Winsorize=0.995、random_state=42、n_init=10）**:

  | 方法 | 维度 | 仅 RFM | RFM + 扩展 | 界面是否用 |
  |:---|:---:|:---:|:---:|:---|
  | **组合特征 composite** `[R_rank, R_rank+F_rank+M_rank]` | 2 / 4 | **0.4486** | 0.3034 | ✅ 配「仅 RFM」 |
  | **PCA 降维**（5 个排名特征取前 2 主成分） | 2 | 0.4369 | **0.3967** | ✅ 配「RFM + 扩展」（默认） |
  | 百分位排名 rank（R/F/M 三列独立） | 3 / 5 | 0.3898 | 0.2857 | ❌ 保留在模块里供复现，UI 不可达 |
  | 对数变换 log（`log1p` 三维） | 3 / 5 | 0.3326 | 0.2750 | ❌ 同上 |

- **配对逻辑（这就是侧边栏少一个控件的原因）**: 每个特征集配它在该特征集下轮廓系数最高的方法——仅 RFM 时组合特征略高（0.4486 vs 0.4369）；加两个扩展特征后 **PCA 高 0.093**（0.3967 vs 0.3034），因为组合特征吃 5 维会退化成 4 维、且两维仍不正交。被淘汰方法的败因: 对数变换压缩不足（F/M 偏度 ~10/~24）；百分位排名保留了 F/M 相关性，距离里 F/M 被隐式放大。
- **共用流程**: Winsorize → `rank(pct=True)` 压到 0-1 → StandardScaler →（仅 PCA）取前 2 个主成分作聚类空间 → K-Means。**PCA 后刻意不再二次标准化**: 主成分按方差降序，拉平会把"PC1 承载信息最多"这个权重丢掉（实测不拉平轮廓系数更高）。
- ⚠️ **必须坦白的取舍**: 组合特征的 `R_rank` 同时是第 2 维的组成部分，展开距离 `d² = 2·ΔR² + ΔF² + ΔM² + 2ΔRΔF + 2ΔRΔM`，即 R 的权重约为 F/M 的 2 倍且带两个正交叉项，两维相关约 0.6、**非严格正交**。这是"用简单性换可解释性"的自觉选择，被追问时主动承认，并紧接着说: **所以 5 维输入我们改用了 PCA（两维严格正交、还高 0.093），保留组合特征是因为它在仅 RFM 时更高、语义一眼可读**。

### 🎯 第二步: 聚类结果（5 个 KPI）
- **屏幕**: 聚类数量 / 轮廓系数 / 参与聚类客户 / **输入维度** / 变换方法。
- **代码**: `app.py:1269-1287` · 计算 `cached_run_kmeans()` @ `app.py:433` → `utils/clustering.py:188` `run_kmeans()`。
- **默认配置实测**: 4 组 / **0.3967** / 4,295 人 / **2 维** / PCA 降维。
- 把「输入维度」显式列出来是故意的: 组合特征在仅 RFM 时是 2 维（0.4486），若拿它处理 5 维输入会变成 4 维（0.3034），**不同维度之间的轮廓系数不可直接比大小**，不标出来就会被横向比较。

### 📍 特征空间 2D 可视化
- **屏幕**: 2D 散点（每点=一位客户），X/Y = **聚类实际用的那两个维度**，颜色=簇，悬浮显示 Customer ID 与原始 R/F/M；支持套索/矩形框选，框选后弹出选中客户明细表（`app.py:1367-1370`）；点图例可显隐各簇。
- **代码**: `app.py:1302-1370`（`st.plotly_chart` 带 `selection_mode` @ `:1346`）· 数据来自 `prepare_features()` 返回的 `transformed_df`。
- **轴是什么**: 默认档 = PC1/PC2；切「仅 RFM」= R_rank（时效性）/ RFM_composite（参与度）。PCA 档下页面会额外列一行**解释方差与载荷**（`app.py:1349-1359`），讲轴时用这行比空口解释主成分清楚: **PC1 是"综合价值"轴（五个特征载荷全为正，越往右价值越高）；PC2 在客单价上 +0.75、在时效性上 -0.54，是一条"高客单低频 vs 低客单近期"的对比轴。**
- **多维时的兜底**: 若配置 ≥3 维，图上方会多一个 `st.selectbox` 让选任意两两组合（`app.py:1321`，selectbox 的 `key` 里带特征集名），标题会注明"n 维空间切片"。**当前两档配置实际都是 2 维，所以这个下拉框不会出现**。
- **交互**: 框选选中的客户会进下方明细表；点图例只是显隐某个簇。
- ⚠️ **为什么没有 3D 分群图（别组都有）**: 做过、又删了，不是没实现。早期那张把结果投在原始 R/F/M 三轴上，几何上就不合适: F 与 M 高度相关（Pearson **0.648**，`log1p` 尺度下达 **0.818**）等于同一信息画两遍；且九成客户的 F、M 都挤在轴的最左下，线性刻度下九成样本只占坐标平面约 6% 面积，四团糊在一起。换坐标也治不好——同一套标签在原始 R/logF/logM 视角下轮廓系数只有 **0.1553**，换成"聚类两维 + 金额排名"也才 **0.3612**，而 3D 投影必然丢信息。现在由 **2D 图（聚类真实空间）+ 画像表 + 中心热力图**共同承担。

### 📊 聚类画像 (Cluster Profile)
- **屏幕**: 每簇一行统计表: 分群名 / 客户数 / 客户占比% / 平均R(天) / 平均F(次) / 平均M($) / **平均客单价 / 品类广度**（仅扩展特征档出现）/ 总收入 / 收入占比%。位于左栏，与下面的雷达图**并排**（`st.columns([1, 1])` @ `app.py:1375`）。
- **代码**: `app.py:1378-1393` · 计算在 `utils/clustering.py:188` `run_kmeans()` 的 `cluster_profile`（`groupby('Cluster').agg`），扩展两列由 `extra_features` 参数驱动。
- **默认配置 K=4 实测**:

  | 簇 | R | F | M | 客户数 | 收入占比 | 运营含义 |
  |:---|:---|:---|:---|:---:|:---:|:---|
  | 重要价值客户 | 近期 | 高频 | 高消费 | 1,110 (25.8%) | **75.7%** | 核心 VIP，重点维护 |
  | 新客户 | 近期 | 低频 | 低消费 | 929 (21.6%) | 9.0% | 培养购买习惯 |
  | 流失客户 | 久未购 | 低频 | 高消费 | 975 (22.7%) | 12.2% | 人均 $1,079，值得投放挽留预算 |
  | 沉睡客户 | 久未购 | 低频 | 低消费 | 1,281 (29.8%) | 3.2% | 人均 $213，唤醒成本高 |

- **结论**: **25.8% 的人贡献 75.7% 的收入**——帕累托直接摆在这张表里；"流失客户"人均消费远高于"沉睡客户"，说明挽留预算该投给前者。
- **扩展特征列的用处**: 各簇平均客单价拉开到 **$170 ~ $599**（3.5 倍）、平均品类广度 **15.4 ~ 145.3 种**（9.4 倍）。这就是"新特征确实参与了分群"的直接证据。**判断加特征有没有价值要看这张表，不是去比不同维度数下的轮廓系数**——如果各簇这两列几乎一样，才说明它们只是噪声。

### 🕸️ 雷达图 (Radar Chart)
- **屏幕**: 每簇一个填充多边形，三轴 = R（已反转，越外越近期）/ F / M，面积越大综合价值越高。
- **代码**: `app.py:1396-1432` · `go.Scatterpolar` + `fill='toself'`。
- **算法**: 对画像的 Avg_Recency/Frequency/Monetary 做 Min-Max 归一化到 **[0.15, 1]**（下限 `_RADAR_MIN=0.15`，否则最差簇三轴全归 0、塌到圆心"视觉消失"，相对差异仍保留）；R 反转 `norm = (1+0.15) - norm_R`；某维度各簇完全相同时退化为 0.5（规则三角形）。
- **结论**: 重要价值客户的三角形面积最大，流失客户最小；形状差异也有信息——新客户偏向 R 轴突出但 F/M 低。答辩时这张最直观。

### 📇 各聚类详情
- **屏幕**: 每簇一张卡片: 簇名（C0: 重要价值客户）/ 三个特征标签 / 客户数与占比 / 平均 R、F、M /（扩展档另有）平均客单价与品类广度 / 收入占比 / 簇轮廓系数。
- **代码**: `app.py:1439-1469` · 标签来自 `utils/clustering.py:261` `get_cluster_labels()`；卡片颜色 `card_colors[cluster_id % len(card_colors)]`（15 色与 K 上限对齐，取模兜底所以色数不足只会复用不报错）。
- **算法**: 拿每簇 R/F/M 均值与全体均值比较 → 近期活跃/长期未购 × 高频/低频 × 高消费/低消费 → 匹配 9 种预设业务标签；标签不够时用 16 个兜底名，再不够生成"细分客群 N"，保证 K=15 时每簇都有唯一非 None 名称。**只看 R/F/M，所以加扩展特征不会改变标签逻辑**。
- **结论**: 卡片是把技术结果翻译成业务语言；簇轮廓系数看该簇自身紧密度与分离度，核心客户簇通常较高。

### 🔥 聚类中心热力图 (归一化)
- **屏幕**: 行=簇、列=聚类维度，颜色越绿该维得分越高（Min-Max 到 0-1）。
- **代码**: `app.py:1473-1516` · 数据是 `run_kmeans()` 返回的 `cluster_centers`；列名映射 `col_map` 已登记 PC1/PC2/R_rank/RFM_composite，否则会露出英文列名。
- **屏幕上是哪几列**: 默认「RFM + 扩展 → PCA」是 **PC1 + PC2 两列**；切「仅 RFM → 组合特征」是 **R_rank + RFM_composite 两列**。
- ⚠️ **别把这张图当"新特征有没有参与分群"的证据**: 默认档下原始特征已被压进主成分，中心里**不存在**单独的 AOV/品类广度列，那个问题要看上面 📊 画像表的 `Avg_AOV` / `Avg_N_products` 两列。只有走「扩展特征 + 组合特征」这条非默认路径（需改代码）才会直接出现那两列。
- ⚠️ **正交性**: composite 的两维相关约 0.6、非严格正交（见 expander 那节）；PCA 的两维严格正交。

---

# 页面 5 · 🔗 关联规则分析

### 🔗 关联规则参数（侧边栏，屏幕标题就是这行）
| 控件 | 默认 | 量程 | 行号 | 作用 |
|:---|:---|:---|:---|:---|
| 最小支持度 | 0.02 | **0.005-0.030** | `app.py:1530` | 商品组合至少出现在多少比例的交易里 |
| 最小置信度 | 0.30 | **0.10-0.70** | `app.py:1534` | 买 A 的人有多大比例也买了 B |
| 分析商品数 Top-N | 100 | 50-300 | `app.py:1538` | 只取最高频的前 N 种商品参与挖掘（控计算量） |

量程是**扫出来的不是随手定的**（见下一节）: 原实现支持度上限到 0.10，但实测 ≥0.05 一条规则都挖不出来，可调区间一半以上是空的，现已收窄。

### 4 个 KPI + 挖掘主体（屏幕无小标题，页面第一排卡片）
- **屏幕**: 分析交易数 / 分析商品数 / 频繁项集 / 关联规则数。
- **代码**: `app.py:1551-1575` · `cached_association()` @ `app.py:1551` → `utils/association.py:11` `prepare_basket_matrix()` + `utils/association.py:67` `run_fpgrowth()`（mlxtend FP-Growth + `association_rules`，按 lift≥1 过滤）。
- **默认参数实测**: 15,446 笔交易 / 100 种商品 / **170 个频繁项集 / 115 条规则**，提升度 1.70-17.90。
- **购物篮矩阵**: 按 Invoice 分组 → 取 Top-N 高频商品 → `pd.crosstab` 独热编码 → 列名换成可读描述。

### 🎚️ 阈值范围如何确定 (敏感性扫描)
- **屏幕**: 左规则数热力图（支持度 × 置信度），右规则数随支持度衰减的折线（置信度各一条、支持度对数轴）+ 结论表。
- **代码**: `app.py:1580-1644`（图 `:1596`、`:1610`）· 刻度常量 `SUPPORT_SWEEP`/`CONFIDENCE_SWEEP` @ `app.py:502-503` · 计算 `cached_threshold_sweep()` @ `app.py:507` → `utils/association.py:222` `threshold_sweep()`。
- **算法**: 利用**频繁项集对 support 单调**，只在最低支持度 0.005 上跑**一次** fpgrowth，之后按 support 过滤 + 逐置信度生成规则，不必 8×8=64 个组合各挖一次。
- **推荐区间的推导规则**: 以「规则数仍不少于 10 条」为可操作下限（与详情表滑块下限一致），避免定出"理论上能调、实际上选不出规则"的空区间。
- **实测扫描（Top-100 商品，页面实时算出非手填）**:

  | 支持度 | 0.005 | 0.010 | 0.015 | 0.020 | 0.025 | 0.030 | 0.040 | 0.050 |
  |:---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
  | 最大规则数（跨置信度） | 6,571 | 1,113 | 366 | 140 | 58 | 30 | 4 | **0** |
  | 频繁项集数 | 1,945 | 490 | 259 | 170 | 129 | 115 | 79 | 44 |

  注意两行**变化方向不一样**: 0.040→0.050 项集只从 79 掉到 44，规则却直接从 4 掉到 **0**——剩下的 44 个全是单品集，凑不出"两种商品同时出现"的规则。
- **三条结论**: ① 支持度推荐 **0.005~0.030**（上界因 0.030 以上规则不足 10 条，下界因再低规则爆炸到 6,571 条且大量退化为长尾偶然组合）；② 置信度推荐 **0.10~0.70**；③ **0.050 以上是死区**——一条规则至少要两种商品共现，而本数据集**商品对**的最高支持度只有 **4.67%**，对比最高频**单品**支持度 19.6%，这个落差说明共现很分散，也解释了零售购物篮分析的 min_support 通常取得很低。
- **反直觉的加分点**: 支持度越低规则越多，而且**最高提升度反而更大**（0.005 档最高 lift 41.4，默认 0.02 档 17.90）。但低支持度规则只覆盖几十笔交易，属长尾偶然共现，所以默认取 0.02 不是一味求多——**"定阈值范围"要平衡的就是规则数、规则强度和统计可靠性**。
- **⚠️ 支持度 2%-4.7% 是不是太低**: 不低。4,000+ 种商品里任两种同时出现的概率天然小；2% 支持度 ≈ 309 笔交易同时含这两件，作为统计证据已足够；经典论文（几千 SKU）也常用 0.5%-2%。真正衡量关联强度的是提升度。

### 📊 规则质量分布 (支持度 × 置信度 × 提升度)
- **屏幕**: 气泡散点（X=支持度，Y=置信度，气泡大小与颜色=提升度，Viridis），悬浮显示完整中文规则。
- **代码**: `app.py:1714-1734`。
- **结论**: 右上角=又常见又可靠，最值得做；颜色越亮说明两商品同现越不是偶然（lift>1 = 正相关）。

### 🕸️ 商品关联网络图
- **屏幕**: 节点=商品（中文标签），节点大小=该商品参与的规则数，连线=两商品间存在规则，悬浮连线看提升度/置信度。
- **代码**: `app.py:1741-1806` · 计算 `utils/association.py:155` `build_network_graph()`（networkx DiGraph 取 lift 最高 30 条规则 + `spring_layout` 坐标）。
- ⚠️ **呈现口径**: 连线用 `go.Scatter(mode='lines')` 画，**没有箭头，是无向展示**——这是刻意的: 强关联通常互为前后项（粉色餐具套装↔蓝色餐具套装双向 lift 同为 17.90），画箭头只会得到两条完全重叠、方向相反的线。逐条规则的方向以散点图和下方详情表的「前项 → 后项」为准。
- **结论**: 一眼看出"枢纽商品"和"商品簇"——三色迷你圆点餐具套装成三角形强关联簇，超大购物袋系列另一个簇；簇内适合捆绑促销或相邻陈列。

### 🔥 商品共现热力图 (Top 20)
- **屏幕**: 20×20 矩阵，颜色 = 行商品出现时列商品也出现的条件概率 P(列|行)，对角线恒为 1。
- **代码**: `app.py:1811-1825` · 计算 `utils/association.py:124` `compute_cooccurrence_matrix()`（`sub.T @ sub` 计数 → 除以行自身出现次数）。
- **结论**: 与网络图互补——热力图看"全局共现强度"，网络图看"整体结构与枢纽"。非对角高值区就是推荐位与套装候选。

### 📋 关联规则详情 (按提升度排序)
- **屏幕**: 规则表（中文规则 / 前项 / 后项 / 支持度 / 置信度 / 提升度）+ 页面内"显示规则数量"滑块（10-100）+ 收起的「📖 指标含义说明」expander。
- **代码**: `app.py:1832-1849`（expander `:1891`）· 中文翻译字典 `PRODUCT_CN`（52 个高频商品）。
- 规则不足 10 条时滑块不渲染（滑块下限高于总数会报错）。
- **三个指标（答辩必问）**: 支持度 = P(A∪B) 普遍性；置信度 = P(B|A) 可靠性；**提升度 = 置信度 ÷ P(B)**，>1 正相关、=1 无关、<1 负相关，**是判断规则有没有意义的最核心指标**。
- **结论**: 默认参数下 Top 规则全部可直觉解释——同系列不同色（三色餐具 lift≈14-18）、配套家具（白色木柜 lift≈14）、同材质首饰盒（≈13）、互补游戏品类（≈11）。

### 🎯 基于关联规则的营销方案
- **屏幕**: 营销动作表（前项 / 后项 / 三指标 / **关联力度** / **建议动作**）+ selectbox 选 5/10/15/20 条 + 分档说明与落地提醒。
- **代码**: `app.py:1854-1870` · 计算 `utils/association.py:285` `build_marketing_actions()`。
- **分档**: lift ≥ 10 强 → 组合套装/配套主推；5 ≤ lift < 10 中 → 货架相邻/详情页同页推荐；lift < 5 弱 → 凑单位/满额赠品候选。
- **为什么按 lift 不按 confidence 分档**: 置信度会被后项自身的高频抬高（热门商品天然容易"被一起买"），lift 才是"比随机共现强多少倍"，直接对应"捆绑的把握有多大"。
- **去重**: A→B 与 B→A 是同一个组合（实测两向 lift 相同），按「前项 ∪ 后项」的商品集合去重，否则同一套装会在方案表里出现两次。
- **结论 + 两点落地提醒**: 默认前 10 个组合绝大多数 lift ≥ 10，可直接当捆绑候选（如三色圆点餐具做套装）。提醒: ① 表里是**商品名**，执行要换成 SKU 并按库存与毛利复核套装定价；② 关联规则只说"一起买"、不辨因果——三色餐具顾客本来就想成套买（做套装是顺势而为），**更有商业价值的是顾客自己没意识到可以搭配的那些组合**。

---

# 页面 6 · 📦 商品与国家调查

> 页面定位（屏幕顶部，`app.py:1913`）: 把两个"看起来平常、实际不好用"的字段变成能用的业务维度——Description 是文本没法直接统计，Country 高度集中（英国占 91% 的行、84.7% 的收入）方差太小。

### 🏷️ 商品描述 (Description) 分析
- **屏幕**: 左各品类词收入贡献横向柱（Y 轴标签「英文词 · 中文含义」，颜色=平均单价），右「销量 vs 平均单价」气泡散点（气泡=收入，X 轴对数），下方数据表（品类词/含义/词类/商品数/收入/收入占比/销量/平均单价），顶部滑块 8-25 个词，再下面收起的「📖 品类词对照表」。
- **代码**: `app.py:1918-1984`（滑块 `:1929`，图 `:1944`、`:1951`，表 `:1957`，对照表 `:1960-1964`）· 计算 `cached_description_words()` @ `app.py:518` → `utils/product_country.py:84` `description_word_stats()`；中文注释词典 `WORD_CN` @ `utils/product_country.py:39`（30 个词）。
- **算法（5 步）**: ① 先聚合到**商品粒度**（StockCode+Description），避免热销商品把某个词的频次刷爆；② 正则 `[A-Z][A-Z'\-]{2,}` 提取全大写、长度 ≥3 的候选词（`utils/product_country.py:25` `_extract_words()`）；③ 过滤 PACK/SET/WITH 这类功能词（`_STOPWORDS` @ `:19`），保留真品类词；④ 汇总商品数/收入/占比/销量/均价并按收入降序；⑤ 关联 `WORD_CN` 补中文含义与词类，未登记的词由 `_word_cn()` 归入"其他"。
- **中文含义是逐词核对真实商品名写的**，例: T-LIGHT→茶蜡托、HOT/WATER/BOTTLE→热水袋（来自 HOT WATER BOTTLE）、UNION→英国国旗图案。
- **结论**: 收入贡献最高是 **RED（红色系列，覆盖 271 种商品、$918,253、占全站 10.62%）**，其后 BAG / HEART / WHITE / PINK / HOLDER，都是居家装饰与节日小商品的高频描述词。
- **词类构成（页面动态算）**: 默认 15 词 = 品类 5 / 颜色 3 / 形态 2 / 图案 2 / 风格 1 / 材质 1 / 规格 1；拉到 25 词 = 品类 7 / 颜色 5 / 图案 3 / 材质 3 / 形态 2 / 风格 2 / 规格 2 / 主题 1。**数量上品类型最多，但颜色词的收入排名明显更高——前五名占 3 席（RED/WHITE/PINK）**，说明顾客高度按"同一个商品的不同花色"选购。这与页面 5 互相印证: 关联最强的正是同系列不同色的低单价小商品（三色餐具 lift≈18）。
- ⚠️ **口径（必读）**: 一个商品会命中多个词（`JUMBO BAG PINK WITH WHITE SPOTS` 含 JUMBO/BAG/PINK/SPOTS），所以**各词收入互相重叠、不能相加当总量**；最典型的是 HOT/WATER/BOTTLE 三个词其实来自同一个商品名 HOT WATER BOTTLE，RETRO/SPOT/RETROSPOT 也高度重叠——这正是为什么要给每个词标含义。另外描述缺失的 2,928 条（0.56%）已在清洗阶段移除。
- ⚠️ **一个容易被误读的点**: 各词**平均单价差异其实很小**（Top-15 落在 $1.26 ~ $2.15），别把"平均单价高"讲成"高客单价品类"。该店的品类差异主要体现在**销量规模**上，所以右图 X 轴取对数才看得清: 右下角是"走量"型（RED/PINK 这类覆盖商品多、总销量大的颜色系列词），左上角是"量小价略高"的词（如 VINTAGE）。

### 🌍 用户所在国家 (Country) 分析
- **屏幕（三层）**: ① 4 个 KPI（覆盖国家数 / 英国收入占比 / 海外客户数 / 海外平均客单价）；② 左「本土 vs 海外 客户画像」表 + 右「客户占比 vs 收入占比」对比柱；③ 「📋 国家明细 (按收入排序)」表（selectbox 10/15/20/30）+ 「📈 收入 Top 5 国家的月度收入构成」堆叠面积图。
- **代码**: `app.py:1987-2073`（KPI `:2005-2011`，画像表 `:2023`，对比柱 `:2037`，明细表 `:2044-2051`，月度图 `:2060-2073`）· 计算 `cached_country_summary()`/`cached_uk_overseas()`/`cached_country_monthly()` @ `app.py:524/530/536` → `utils/product_country.py:134 country_summary()`、`:164 uk_vs_overseas_profile()`、`:193 country_monthly_revenue()`。
- **算法**: 国家汇总含**复购率**（该国内发票数 > 1 的客户占比，用来区分"一次性买家聚集地"和"稳定复购市场"）；本土 vs 海外按客户所属国家二分，对比 R/F/M + AOV + 品类广度均值与收入占比。
- **为什么 Country 不做聚类特征（答辩重点）**: 英国占 91% 的行、84.7% 的收入，方差太小，塞进 K-Means 等于加了一个近似常数的维度，几乎不产生区分度。但它作为**分析维度**很有价值，所以本页改用「本土 vs 海外」的二分对比来挖。
- **结论**: 只看收入占比会漏掉一个事实——**海外人少但单体价值明显更高**: 340 人（7.9% 客户）贡献 15.3% 收入，平均消费 $3,880 vs 英国 $1,852、平均客单价 $662 vs $348，约为本土的 1.9~2.1 倍；海外多为批发型小客户。运营含义: 本土做频次与复购，海外做客单价与批发阶梯价。复购率列也能看出差别（德国 70.1%、法国 76.1% > 英国 67.1%）。
- ⚠️ **口径（页面已加 caption，`app.py:2052-2058`）**: 国家明细表的「客户数」是**每个国家内部**的唯一客户数，同一客户在两国都下单会在两国各计一次。本数据集有 **5 位**这样的客户（12417/12422/12423/12431/12745），所以整列合计 **4,300 ≠ 全站唯一客户 4,295**。要"客户总数"请用上方本土 vs 海外那张表——它按客户的**首个**国家二分，每人只计一次（3,955 + 340 = 4,295 ✓）。两种口径都对，回答的问题不同: 按国家看规模用前者，按客户看归属用后者。

---

## 交互功能一览（含控件行号）

| 屏幕位置 | 控件 | 代码 | 效果 |
|:---|:---|:---|:---|
| 侧边栏（全局） | 6 页导航 Radio | `app.py:544` | 切页 |
| 页面 2 顶部 | 时间范围 expander + 两个下拉 | `app.py:785-796` | 只联动本页分段图与热销排名 |
| 页面 2 热销区 | Top-N 滑块 5-30 | `app.py:878` | 榜单条数与图高联动 |
| 侧边栏（页面 4） | K 范围 / Winsorize / K / 输入特征集 | `app.py:1135-1148` | 全页聚类图表跟着变 |
| 页面 4 2D 图 | 套索/矩形框选 | `app.py:1346` | 弹出选中客户明细表 |
| 页面 4 2D 图 | 图例点选 | Plotly 自带 | 显隐某个客户群 |
| 页面 4 2D 图 | 维度切片下拉 | `app.py:1321` | 仅配置 ≥3 维时出现（当前两档都是 2 维，不会出现） |
| 侧边栏（页面 5） | 支持度 / 置信度 / Top-N | `app.py:1530-1538` | 重挖规则并联动全页 |
| 页面 5 详情表 | 显示条数滑块 10-100 | `app.py:1847` | 规则不足 10 条时自动隐藏 |
| 页面 5 营销区 | 方案条数 selectbox | `app.py:1862` | 5/10/15/20 条动作 |
| 页面 6 描述区 | 品类词数量滑块 8-25 | `app.py:1929` | 控制显示词数 |
| 页面 6 描述区 | 品类词对照表 expander | `app.py:1960` | 查全部 30 词的含义与词类 |
| 页面 6 国家区 | 显示数量 selectbox | `app.py:2045` | 国家明细 10/15/20/30 行 |

---

## 答辩问答速查

| 会问什么 | 怎么答（详细依据见对应小节） |
|:---|:---|
| 为什么 K=4？为什么不做经典五类？ | 统计上 K≥3 里最高（默认档 0.3967，K=3 0.3844 / K=5 0.3825），肘部也在 4-5；业务上正好分出 VIP/新客/流失/沉睡四类且各有动作。K=5 只会把中间态客户切成两个边界模糊的簇。滑块可现场调到 5 验证。→ 页面 4「第一步」 |
| 轮廓系数 0.40 算高吗？ | 学术基准 <0.25 差、0.25~0.4 一般可用、0.4~0.6 良好；默认配置 **0.3967** 接近良好。这份数据本身是连续谱: ① 100% 的客户都离本簇中心更近（**没有错分**），但"到最近他簇 ÷ 到本簇"中位数只有 0.43（约 2.3 倍），簇间天然重叠；② 负轮廓点仅 **0.7%**；③ PCA 前两主成分累计 **82.1%** 方差（PC1 62.6%），数据是连续纺锤形不是几个团块。所以 0.40 是数据属性不是算法问题。 |
| 那能不能更高？ | 能——K 降到 2 就有 0.4587（跨过 0.4），但只是粗分"活跃/沉睡"，对精细化运营没意义，这就是 ⭐ 限制在 K≥3 的原因。 |
| 加特征后轮廓系数反而降，怎么解释？ | 维度升高的系统性现象（高维欧氏距离趋于均匀），**不同维度数之间不可横向比**。同方法只改维度的三条阶梯: composite 2D 0.4486→4D 0.3034、rank 3D 0.3898→5D 0.2857、log 3D 0.3326→5D 0.2750。判断价值要看画像表: 各簇平均客单价 $170~$599（3.5 倍）、品类广度 15.4~145.3 种（9.4 倍）→ 确实参与分群。而且我们没有在数值上让步: 5 维改用 PCA 得 0.3967，比 composite 的 0.3034 高 0.093。 |
| 为什么"输入特征集"里带方法，不单独选？ | 每档都配它实测最优的方法（仅 RFM→composite 0.4486 > PCA 0.4369；RFM+扩展→PCA 0.3967 > composite 0.3034）。少一个控件、演示时也不会出现"方法和特征集不搭"的组合。 |
| 除 RFM 外为什么加这两个特征、怎么选的？ | 7 个候选全算过，两条硬门槛: 与 R/F/M 弱相关、分布可控。入选 AOV（与 R/F/M 最高才 0.33）与品类广度（与 F 0.66 但语义正交），两者彼此仅 0.16；否决活跃月数（与 F 0.70）、退货次数（0.73 且 60% 零值）、生命周期跨度（34.6% 结构零值）、单次件数（偏度 45.7）、退货率（定义失真）。→ 页面 3「🧩 扩展客户特征」 |
| 你的两个聚类维度正交吗？ | 承认 + 给方案: composite 的 R_rank 与 RFM_composite 共享 R 项、相关约 0.6、**非严格正交**（R 权重约 F/M 两倍）；这是用简单性换可解释性；**要严格正交我们本来就备了 PCA，且 5 维输入默认就走 PCA**。 |
| 支持度/置信度量程怎么定的？ | 64 组合网格扫描 + 以"规则数 ≥10"为下限反推: 支持度 0.005~0.030、置信度 0.10~0.70。关键发现是 ≥0.05 是死区——商品对最高支持度只有 4.67%（单品 19.6%）。原实现上界 0.10 一半以上是空的，已按实测收窄。 |
| 支持度 2%~4.7% 是不是太低？ | 不低，是商品种类多的必然结果（4,000+ SKU，Top-100 里任两种同现概率天然小）；2% ≈ 309 笔交易的共同出现，作为证据足够；经典论文也用 0.5%~2%。衡量强度看提升度，Top 规则 lift 17.9。 |
| 清洗后平均数量为什么反而 +3.5？ | 清洗前后不是同一个总体，加权平均的算术必然。被删 125,862 行整体平均 -0.66 件；按步归因三分之二来自"去缺失客户 ID"（+2.245）。单价反向从 $4.98 降到 $3.01 是同一机制（被删行均价 $10.02）。→ 页面 1「📊 清洗前后数据对比」 |
| 国家为什么不做聚类特征？ | 方差太小（英国占 91% 行、84.7% 收入）≈ 常数维度，不产生区分度；改作分析维度用「本土 vs 海外」二分，差异很明显（海外 340 人贡献 15.3% 收入、客单价 1.9 倍）。 |
| 文本字段 Description 怎么处理？ | 清洗先移除缺失描述（2,928 条 0.56%）→ 商品粒度上用正则提高频全大写词、过滤功能词 → 统计每词收入/销量/商品数/均价，并为 30 个词配人工核对的中文含义与词类。口径要点: 一词多命中、各词合计不可相加。 |
| 为什么用分段柱状图不用直方图？为什么没有 3D 散点图？ | 直方图被极端离群值拉长 X 轴、主体挤在左侧看不清，且评委习惯"分段+占比"。3D 图**做过又删了**: F/M 原始 Pearson 0.648、log1p 0.818 等于同一信息画两遍，九成样本只占平面 6% 面积；量化过 4 版最好的也只有 0.3612（原始 R/logF/logM 仅 0.1553），投影还必然丢信息，所以改由 2D（聚类真实空间）+ 画像表 + 中心热力图承担。 |
| Winsorize 是什么、为什么做？ | 把超过指定分位的极端值截断到该分位值（这里截 F/M 的 top 0.5%）。不截，买 500 次/消费 $20,000+ 的少数客户会把 K-Means 质心拉偏。 |
| 时间筛选器为什么只在数据探索页？ | RFM/聚类需要完整交易历史算 R/F/M，截一段会把活跃客户误判成新客；探索页的分段图与热销榜才是适合看时段差异的。 |
| 数据怎么清洗的？ | 7 步（取消单→非商品→缺描述→负数量→零负价→重复→缺客户ID），525,461→399,599（-23.95%）；页面 1「✅ 清洗后数据质量报告」的复检表把 7 项在清洗后重算一遍、全部为 0。 |

---

## 代码索引

**项目文件**: `app.py`（Streamlit 主应用，6 页 if/elif 分支，2,099 行）· `utils/` 5 个纯计算模块（无任何 Streamlit UI 代码，输入 DataFrame → 输出 DataFrame/dict，便于单测与复用）· `week1_explore.py`（第一周独立探索脚本，非大屏组件）· `requirements.txt` · `online_retail_II(1).xlsx` + 两个 Parquet 缓存 + `.cache_meta.json` 均随仓库提交 · `大屏图表说明文档.md`（本文档）。

### app.py 里的辅助函数

| 函数 | 位置 | 作用 |
|:---|:---|:---|
| `_cache_fingerprint(filepath)` | `:231` | 算 xlsx + `utils/data_cleaner.py` 的内容指纹，判断 Parquet 缓存是否失效 |
| `load_and_clean(filepath)` | `:248` | 数据总入口: 读数据 → 清洗 → 质量报告 → 组装 `rfm_df`/`rfm_scored`/`rfm_stats` |
| `filter_explore_data(_df, start, end)` | `:298` | 页面 2 时间筛选的局部副本 |
| `compute_distribution_stats(_df)` | `:304` | Quantity/Price/Revenue 的分段计数与分位数 |
| `compute_top_products(_df, top_n)` | `:337` | Top-N 热销商品聚合 |
| `compute_rfm_segments(_rfm_df)` | `:348` | R/F/M 各维分位数与分段 |
| `_fmt_val` / `_fmt_range` / `stat_cards_row` / `_dominant_bin` | `:388` `:398` `:408` `:416` | 数值格式化、统计卡片行 HTML、取占比最高档（供第 3 页文案动态引用） |
| `cached_prepare_and_elbow(...)` | `:423` | 特征工程 + 肘部/轮廓 K 扫描 |
| `cached_run_kmeans(...)` | `:433` | K-Means 拟合与画像聚合 |
| `cached_compare_methods(...)` | `:440` | composite/pca 两方法在同一 K 下对比（供 expander） |
| `compute_extended_segments(_rfm_df)` | `:466` | AOV / 品类广度分段统计（页面 3） |
| `cached_threshold_sweep(_df, top_items, _n_rows)` | `:507` | 阈值网格扫描（页面 5） |
| `cached_description_words` / `cached_country_summary` / `cached_uk_overseas` / `cached_country_monthly` | `:518` `:524` `:530` `:536` | 页面 6 的四块计算 |
| `assign_segment(row)` | `:1060` | 页面 3 的规则分群标签 |
| `cached_association(...)` | `:1565`（定义在页面 5 分支内） | FP-Growth 挖掘 + 共现矩阵 |
| `_cn(name)` / `_cn_join(names)` | 页面 5 分支内 | 商品英文名 → 中文，后者逐个翻译 `A + B` 组合 |

**全局常量**: `COLORS`（`:179`，palette 15 色与 K 上限对齐）· `QUALITY_ITEM_KEYS`/`QUALITY_EXTRA_KEYS`（`:195`/`:204`，质量问题「中文名+键名」唯一来源，页面 1 三处展示共用）· `CHART_LAYOUT`（统一白色背景/微软雅黑/hoverlabel）· `EXT_FEATURE_SET_METHOD`（`:226`，`{'rfm_only':'composite','rfm_ext':'pca'}`，**特征集→方法的自动配对表**）· `SUPPORT_SWEEP`/`CONFIDENCE_SWEEP`（`:502`/`:503`）· `PRODUCT_CN`（52 个高频商品中文名，定义在页面 5 分支内 `:1671-1724`）。

### utils/ 五个模块

| 文件 | 函数（位置） | 作用 |
|:---|:---|:---|
| `data_cleaner.py` | `load_raw_data` `:25` | 读 xlsx、解析日期 |
| | `get_data_quality_report` `:31` | 7 类问题计数 + 占比（页面 1 饼图用其中 6 类，复检表用全部 7 类） |
| | `clean_data` `:49` | 7 步清洗 + 生成 Revenue（步骤行号见页面 1 那节） |
| | `get_cleaning_summary` `:107` | 前后行数/移除比例/客户数/商品数/日期范围 |
| `rfm.py` | `calculate_rfm` `:9` | 按客户聚合 R/F/M（快照日 = max(InvoiceDate)+1 天） |
| | `add_rfm_scores` `:37` | 三维各 1-5 档（统一先 `rank(method='first')` 再等频分箱；R 反转）+ RFM_score |
| | `get_rfm_stats` `:71` | 客户数/均值/中位数/总收入 |
| | `calculate_extended_features` `:102` | AOV 与品类广度；文档串里记着选型依据与被否候选的原因 |
| | `EXTENDED_FEATURE_INFO` `:86` | 两个扩展特征的中文名/算法/含义，页面表格与画像列名共用 |
| `clustering.py` | `prepare_features` `:33` | Winsorize→百分位排名→标准化（→PCA 前两主成分）。模块支持 composite/**pca**/rank/log **四种方法，界面只用前两种**（rank/log 实测更差，保留便于复现对比，见 `:4-14` 的模块文档与实测数字）；`extra_features` 以 `_rank` 列追加、不动原 R/F/M 路径 |
| | `find_optimal_k` `:149` | 扫描 K 记 inertia 与 silhouette；`recommended_k` = K≥3 中最高 |
| | `run_kmeans` `:188` | 完整流程: 特征工程 → KMeans(n_init=10, max_iter=500) → 画像聚合 → 整体/分簇轮廓系数；返回含 `pca` 对象（仅 PCA 档非 None）供页面显示解释方差与载荷 |
| | `get_cluster_labels` `:261` | 三层标签保障: 9 种规则标签 → 16 个兜底名 → "细分客群 N"；只看 R/F/M，扩展特征不影响标签 |
| `association.py` | `prepare_basket_matrix` `:11` | 按发票分组 → Top-N 商品 → crosstab 独热 → 列名换描述 |
| | `run_fpgrowth` `:67` | 频繁项集 + `association_rules` + 按 lift 过滤排序 |
| | `compute_cooccurrence_matrix` `:124` | `sub.T @ sub` → 除以行对角元 → 条件概率 P(列\|行) |
| | `build_network_graph` `:155` | DiGraph（内部保留方向与指标）+ spring_layout 坐标 |
| | `threshold_sweep` `:222` | 只挖一次的敏感性扫描；返回扫描表 + `max_item_support`（19.6%）/`max_pair_support`（4.67%，规则支持度的天然上界） |
| | `build_marketing_actions` `:285` | 按 lift 分档翻译成动作，并按「前项∪后项」集合去重 |
| `product_country.py` | `description_word_stats` `:84`（+ `_extract_words` `:25`、`_STOPWORDS` `:19`、`WORD_CN` `:39`、`_word_cn` `:79`） | 商品粒度提词 → 品类词统计 → 补中文含义与词类。**各词合计互相重叠，不可相加** |
| | `country_summary` `:134` | 按国家汇总，含复购率（国内发票数 >1 的客户占比） |
| | `uk_vs_overseas_profile` `:164` | 按客户**首个**国家二分，对比 R/F/M + AOV + 品类广度与收入占比（每人只计一次） |
| | `country_monthly_revenue` `:193` | 收入 Top-N 国家的月度透视表，供堆叠面积图 |

**数据流向（一次完整链路）**:
```
xlsx / Parquet → cleaned_df (399,599 行)
  ├─ calculate_rfm + calculate_extended_features → rfm_df (5 维客户级) → 页面 3
  │     └─ prepare_features(extra_features=[...]) → K-Means → 画像/标签 → 页面 4
  ├─ prepare_basket_matrix → fpgrowth → 规则 → 共现/网络/营销方案 → 页面 5
  └─ description_word_stats / country_summary / uk_vs_overseas_profile → 页面 6
```
