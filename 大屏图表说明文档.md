# 在线零售客户分群分析 — 大屏图表说明文档

> 课程: 大数据分析应用综合实践  
> 数据集: UCI Online Retail II (英国在线零售商 2009.12 - 2010.12)  
> 技术栈: Streamlit + Plotly + scikit-learn + mlxtend + networkx + pandas  
> 在线地址: https://online-retail-customer-segmentation-eswdabwlnoaqev3rzvrhxq.streamlit.app/

---

## 整体架构

大屏共 6 个页面，按数据分析流程递进排列:

| 页面 | 主题 | 定位 |
|------|------|------|
| 📈 数据概览 | 数据理解、清洗、基础统计 | 数据质量保障与整体画像 |
| 🔍 数据探索 | 字段分布、热销商品 (支持时间筛选) | 描述性统计与业务洞察 |
| 👥 客户特征分析 | R/F/M + 两个扩展客户特征 (AOV、品类广度) | 特征工程与业务分群 |
| 🎯 K-Means 聚类 | 机器学习分群 (K=4 · 双特征集各配最优变换方法) | 核心算法与结果展示 |
| 🔗 关联规则分析 | FP-Growth 商品共购挖掘 + 阈值确定 + 营销方案 | 购物篮分析与推荐策略 |
| 📦 商品与国家调查 | 商品描述 (文本→品类) 与用户国家两个特征的业务探索 | 特征理解与市场洞察 |

数据来源: `online_retail_II(1).xlsx`，共 525,461 条原始记录，8 个字段 (Invoice, StockCode, Description, Quantity, InvoiceDate, Price, Customer ID, Country)。

**数据使用策略**: 除页面 2 (数据探索) 提供时间筛选器外，其余页面均始终使用完整清洗后数据。页面 2 的筛选只影响本页面的分段统计图和热销商品排名。

---

## 侧边栏

**展示内容**:
- 导航面板: 6 个页面的 Radio 选择器
- 数据集信息: 名称、时间范围 (2009-12-01 ~ 2010-12-09)、清洗后记录数 (399,599) 和客户数 (4,295)

**答辩解释**: 侧边栏简洁展示数据集基本信息，不设置全局筛选器，避免 RFM 和聚类等昂贵计算被反复触发。页面 4/5 的参数控件挂在侧边栏，切换页面时自动出现/消失。

---

## 页面 1: 📈 数据概览

### 1.1 快速概览 KPI (3 列布局)

| 指标 | 含义 | 怎么来的 |
|------|------|----------|
| 原始记录数 | Excel 中总行数 | `len(raw_df)` = 525,461 |
| 时间跨度 | 数据覆盖的日期范围 | 自定义宽幅卡片 (date-card) 展示，2009-12-01 ~ 2010-12-09 |
| 覆盖国家 | 客户所在国家数量 | `raw_df['Country'].nunique()` = 40 个 |

**怎么来的**: 原始记录数和覆盖国家使用 `st.metric`，时间跨度使用自定义 HTML 卡片 (`date-card` CSS 类)，列宽比为 1:2:1，确保日期完整显示不被截断。

**答辩解释**: 这是对数据集最基本的概括，展示数据规模和覆盖范围。英国为主力市场 (占比 90%+)，另有 39 个海外国家。

### 1.2 原始数据质量饼图

**图表类型**: Plotly 环形饼图 (hole=0.45)

**展示内容**: 6 类数据质量问题的数量占比:
- 取消订单 (Invoice 以 'C' 开头)
- 缺失客户 ID (Customer ID 为空)
- 负数量 (退货记录)
- 零/负价格 (赠品或系统错误)
- 精确重复行
- 非商品编码 (POST, BANK CHARGES 等)

**怎么来的**: 通过 `get_data_quality_report(raw_df)` 函数逐项统计。每一项都是 `df[条件].shape[0]` 计数，百分比 = 计数 / 总行数 × 100。

**答辩解释**: 饼图直观展示了数据的主要质量问题。其中缺失客户 ID 占比最大 (约 20.5%)，这是因为部分交易为游客下单未注册账号。取消订单也占相当比例。这些质量问题必须在分析前清洗掉，否则会严重影响 RFM 和聚类结果的准确性。

> ⚠️ **口径提醒 (易被追问)**: 6 类问题**互不互斥**——同一行记录可能既是被取消订单、又是负数量。因此饼图展示的是 143,783 条「问题命中次数」的构成 (各扇区之和 = 100%)，而左侧文字里的百分比分母是 **525,461 条总行数**。两处口径不同，文字百分比之和当然不等于 100%，这不是计算错误。若被追问，可回答"饼图看问题类型的相对构成，文字看各类问题占全量数据的比重"。

### 1.3 数据清洗步骤表格

**展示内容**: 7 步清洗流程的 Markdown 表格

**怎么来的**: 代码中 `clean_data()` 函数按顺序执行 7 个过滤步骤:

1. 移除取消订单 → `~df[Invoice].str.startswith('C')`
2. 移除非商品编码 → `~df[StockCode].isin(['POST', 'BANK CHARGES', ...])`
3. 移除缺失描述 → `df.dropna(subset=['Description'])`
4. 移除负数量 → `df[Quantity > 0]`
5. 移除零/负价格 → `df[Price > 0]`
6. 去除精确重复 → `df.drop_duplicates()`
7. 移除缺失客户 ID → `df.dropna(subset=['Customer ID'])`

**答辩解释**: 清洗遵循从粗到细的原则。先处理业务层面问题 (取消订单、非商品)，再处理数据层面 (缺失、异常值、重复)。每一步都有明确的业务理由——比如负数量是退货，和取消订单对应，不应纳入正向销售分析。

### 1.4 清洗结果 KPI (4 个指标)

| 指标 | 数值 | 计算方式 |
|------|------|----------|
| 清洗前 | 525,461 | `len(raw_df)` |
| 清洗后 | 399,599 | `len(cleaned_df)` |
| 移除 | 125,862 | 清洗前 - 清洗后 |
| 移除比例 | 23.95% | 移除 / 清洗前 × 100 |

**答辩解释**: 约 24% 的数据被清洗掉，主要来自缺失客户 ID (约 20%) 和取消订单 (约 2%)。移除比例在合理范围内，清洗后仍保留近 40 万条记录用于分析。

### 1.5 清洗前后数据对比 (分组柱状图 + 对比表)

**图表类型**: Plotly 分组柱状图 (px.bar，color=阶段) + `st.dataframe` 对比表

**展示内容**: 上方柱状图对比清洗前后的 4 项数据规模指标 (交易记录数、客户数、商品数、发票数)，清洗前红色、清洗后绿色，柱顶标注具体数值。下方附一张 7 行详细对比表，额外包含平均数量、平均单价、时间范围及各项变化量。

**怎么来的**: 分别对 `raw_df` 和 `cleaned_df` 计算 `len()`、`nunique('Customer ID')`、`nunique('StockCode')`、`nunique('Invoice')`、`Quantity.mean()`、`Price.mean()` 等统计量，整理为 DataFrame 后绘制。

**答辩解释**: 这张图是清洗效果的"数据规模前后对照"。清洗前 52.5 万条记录、4,383 客户、4,632 商品、28,816 张发票；清洗后 39.9 万条、4,295 客户、4,011 商品、19,011 张发票。可以直观看到: 记录减少约 24% (主要来自缺失客户 ID 和取消订单)，发票数减少最多 (约 34%，因为很多无效发票被整张移除)，而客户数只减少 88 人 (2%)——说明清洗主要移除的是无效交易而非真实客户。平均数量从 10.3 升到 13.8、平均单价从 $4.98 降到 $3.01，这一升一降的同一套解释见下。

#### 为什么清洗后平均数量反而 +3.5? (易被追问)

这不是数据出错，而是**加权平均的算术必然结果: 删掉一批均值低于整体的行，剩下的均值一定上升**。被删的 125,862 行整体平均数量只有 **-0.66 件** (原始整体是 10.34)。按 7 个清洗步骤**逐步归因** (每行只计入实际把它删掉的那一步，因此互不重叠):

| 步骤 | 移除行数 | 该批行的平均数量 | 对整体均值的抬升 |
|:---:|---:|---:|---:|
| 1 去取消订单 | 10,206 | **-20.9 件** | **+0.619** |
| 2 去非商品编码 | 2,227 | +3.9 件 | +0.031 |
| 3 去缺失描述 | 2,922 | -22.1 件 | +0.189 |
| 4 去负数量 | 294 | **-523.8 件** | +0.309 |
| 5 去零/负价格 | 464 | +63.7 件 | **-0.048** |
| 6 去精确重复 | 6,830 | +2.8 件 | +0.118 |
| 7 去缺失客户 ID | 102,919 | **+2.84 件** | **+2.245** |
| **合计** | **125,862** | **-0.66 件** | **+3.463** (10.34 → 13.80) |

抬升最大的两步是 **第 7 步「去缺失客户 ID」**(剔除 102,919 条小额游客单，平均仅 2.84 件，**+2.25，占全部 +3.46 增量的三分之二**) 与 **第 1 步「去取消订单」**(10,206 条退货单平均 -20.9 件，**+0.62**)。第 4 步删掉的行平均数量最极端 (-523.8 件)，但只有 294 行，权重不够，只贡献 +0.31。第 5 步是唯一**拉低**均值的一步 (-0.048)——那 464 行平均 63.7 件，高于当时的整体均值。

> **与 §1.4 质量报告的口径差别 (易被追问)**: §1.4 按「问题类型」计数，同一行可能同时命中多类 (既是取消单又是负数量)，6 类合计 143,783 > 实际移除的 125,862 行；本表按**清洗步骤**归因，一行只归入真正把它删掉的那一步，所以互斥且合计恰好等于 125,862。两张数不能互相核对，但各自都自洽。

**答辩要点**: 清洗前后**不是同一个总体**——原始 52.5 万行是"全部流水"(含退货、含游客单)，清洗后 39.96 万行是"有客户 ID 的正向销售"。两个均值不可直接比大小，`+3.5` 表示的是**样本构成变化**，不是数据规模或质量变化。

**平均单价反向下降是同一机制**: 被删掉的那批行整体平均单价高达 **$10.02** (含 POST 邮费、BANK CHARGES 银行费用这类高价非商品行，以及缺客户 ID 的高价单)，剔除后单价均值自然被拉低。数量升、单价降是同一枚硬币的两面。

> **⚠️ 三个"清洗前单价"别混 (页面上已用 caption 标出)**: 对比表里的 **$4.98** 只统计 `Price > 0` 的行 (先剔除 3,690 条零/负价格)；全部 525,461 行的均值是 **$4.69**；被删那 125,862 行的均值是 **$10.02**。三者口径不同，因此"下降幅度"取决于用哪个当基线——页面用 $4.98 → $3.01，答辩时若被问"为什么不用全行均值"，答: 零/负价格本身就是待清洗的问题行，把它们算进"清洗前正常单价"会低估单价。而「清洗前平均数量 10.3」用的是**全部行**口径，这两行口径不一致，页面上已明确标注。

### 1.6 清洗后数据质量复检表 (7 行 + 3 个指标)

**展示内容**: 一张「检查项 / 清洗前命中 (条) / 清洗后命中 (条)」三列 7 行的表，加客户数 (4,295)、商品数 (4,011)、记录数 (399,599) 三个指标卡。

**怎么来的**: 把 §1.4 的 6 类问题 (以及缺失描述) 用**同一个** `get_data_quality_report()` 在清洗后的数据上原样重算一遍，与清洗前的计数并排显示。7 项在清洗后全部为 **0**。两处的检查项名称来自同一份常量 `QUALITY_ITEM_KEYS`，不会再出现"饼图一套口径、复检表另一套口径"。

**答辩解释**: 这是数据质量的"双重检查"——不写"已无质量问题"这种口头结论，而是把清洗前后的命中数并排摆出来自己看。它能挡住的质疑是"你是不是只清了占比大的那几类"：7 类里最小的两类 (非商品编码 0.53%、缺失描述 0.56%) 同样归零。

### 1.7 核心指标卡片 (5 个)

| 指标 | 含义 | 计算方式 |
|------|------|----------|
| 交易总量 | 清洗后的交易行数 | `len(cleaned_df)` |
| 客户数量 | 唯一客户 ID 数 | `cleaned_df['Customer ID'].nunique()` |
| 商品数量 | 唯一商品编码数 | `cleaned_df['StockCode'].nunique()` |
| 总收入 | 全部交易的 Revenue 总和 | `sum(Quantity × Price)` |
| 平均订单金额 | 每张发票的平均收入 | `groupby('Invoice')['Revenue'].sum().mean()` |

**答辩解释**: 这 5 个 KPI 是清洗后数据的"体检报告"，概括了业务全貌——约 4,295 客户购买了 4,011 种商品，产生约 864 万美元总收入 (8,644,153)，平均每张发票约 454.69 美元。平均订单金额反映了客户的典型消费水平。

### 1.8 月度收入趋势 (面积图)

**图表类型**: Plotly 面积图 (px.area)

**展示内容**: 2009.12 - 2010.12 每月的总收入变化趋势

**怎么来的**: `cleaned_df.set_index('InvoiceDate').resample('ME')['Revenue'].sum()`，按月末 ('ME') 聚合求和，得到每月总收入。

**答辩解释**: 面积图展示了收入的时间走势。实测月度收入 (万元): 2009-12 **67.80** → 2010 年 1 月 53.73 → **2 月 49.79 (全年低点)** → 3 月 66.60 → 4-6 月在 58~63 之间平台波动 → **7 月 58.15 (小幅回落) → 8 月 59.46** → 9 月 **80.55** → 10 月 **101.16** → **11 月 115.60 (全年最高)**。要点三条: (1) 走势不是匀速增长，而是"**上半年平台 + 9-11 月陡峭冲高**"，拐点在 **9 月**而非夏季，圣诞采购季拉动明显；(2) 7-8 月是 6 月之后的一次小幅回落，**不要说成"夏季回升"**；(3) 下半年确实高于上半年，且这个结论不依赖不完整的 12 月: 2010 年 1-6 月合计 350.9 万，7-11 月 (5 个月) 已达 414.9 万。这个趋势对后续预测和分群有参考价值。

> ⚠️ **看图提醒 (易被追问)**: **12 月的柱子是全图最低的 (约 30.8 万)，这不是业务下滑，而是数据集只覆盖到 2010-12-09**——`resample('ME')` 会把 12 月归到月末，那只是 9 天的收入，属于不完整月份。答辩时应主动说明，避免被理解成"年末暴跌"。

### 1.9 国家收入排名 Top 10 (横向柱状图)

**图表类型**: Plotly 横向柱状图 (px.bar, orientation='h')

**展示内容**: 收入最高的前 10 个国家

**怎么来的**: `cleaned_df.groupby('Country')['Revenue'].sum().sort_values(ascending=False).head(10)`

**答辩解释**: 英国占据绝对主导地位 (占总收入 **84.75%**)，其后依次是 **爱尔兰 (EIRE) 3.88% > 荷兰 3.08% > 德国 2.12%**，均为欧洲国家（EIRE 即爱尔兰，数据集里与 France 是两个独立市场）。地域高度集中说明这是一家主要面向英国本土客户的在线零售商。海外收入占比虽小，但在客户分群分析中可作为辅助维度——用法见第 6 页的「本土 vs 海外」二分对比。

---

## 页面 2: 🔍 数据探索

### 2.0 时间范围筛选器 (页面局部交互)

**展示内容**: 可折叠的 expander，内含起始月/结束月下拉选择器

**怎么来的**:
1. 从完整数据的 InvoiceDate 中提取最小和最大月份
2. 生成所有可选月份列表 (2009-12 到 2010-12)
3. 默认选中全部范围
4. 筛选后创建 `explore_df` (仅本页使用的局部数据副本)

**设计理由**: 时间筛选器仅放在数据探索页，不影响 RFM 和聚类。因为 RFM 衡量的是客户全生命周期价值，聚类需要完整的客户画像——截取部分时间段会破坏这些分析的完整性。数据探索页的分段统计图和热销排名则适合按时间段观察变化。

**答辩解释**: 这是大屏的图表联动交互功能。选择不同时间段可以观察不同季度的交易分布差异——比如 Q4 (10-12月) 节日季的数量/价格分布可能与上半年明显不同。筛选结果只影响本页图表，RFM 和聚类始终使用完整数据。

### 2.1 字段值分布 (3 个分段统计 Tab)

**设计说明**: 早期版本使用学术风格的直方图 (nbins=80)，但存在两个问题: 一是极端离群值会把 X 轴拉得很长、主体分布挤在最左侧看不清; 二是评委更习惯"业务分段 + 占比"的读图方式。因此改为**方案 C: 分段统计图**——把每个字段按业务含义切成若干区间，统计每段的交易行数与占比，用柱状图 + 百分比标签直观呈现。

**整体结构**: Quantity / Price / Revenue 三个 Tab，每个 Tab 由三部分组成:

1. **统计卡片行** (`stat_cards_row`，4 张紧凑等高卡片): 中位数、均值、中间 50% 范围 (下四分位 至 上四分位)、前 10% 阈值。
2. **分段柱状图** (`px.bar`): X 轴为业务区间标签，Y 轴为该区间**交易行数** (一行 = 发票里的一个商品明细行，不是张发票)，柱子颜色由浅到深映射数量多少 (`color_continuous_scale=[主色+66, 主色]`)，每根柱子顶部标注该段占比 `%`。页面 caption 已写明这个单位，避免被读成"订单张数"。
3. **分位数脚注** (一行 Markdown): 下四分位 · 中位数 · 上四分位 · 前 5% 值 · 最大值，用易懂中文替代 P25/P50/P75/P95 术语。

**怎么来的**: `compute_distribution_stats(explore_df)` 一次性算出每个字段的中位数、均值、各分位数以及按业务阈值切分的区间计数 (`labels` / `counts` / `pcts`)。分段柱状图直接用这三组数组绘制。

#### Quantity (购买数量)

**答辩解释**: 购买数量呈明显的右偏分布——**中位数只有 5 件，均值却有 13.8 件**，差距全靠批发大单拉开。实测 (全量清洗数据) 六个分段的占比依次是: **6-12 件 33.7% > 1-2 件 33.5% > 3-5 件 16.9% > 13-24 件 8.2% > 25-50 件 4.8% > 50+ 件 2.9%**。也就是说"1-2 件"与"6-12 件"几乎并列最大 (两档合计 67.2%)，而几百件的批发大单只占极小比例。**讲的时候别顺着"越小的段占比越高"讲**，最大档不是最小那一档——业务上的读法是: 这店既有零散小单 (低价配饰)，也有成批购买 (它是批发型零售)，两类客户混在同一张图上。

#### Price (单价)

**答辩解释**: 单价集中在 0-10 美元区间 ($0-1 20.9% + $1-2 31.4% + $2-5 33.1% + $5-10 11.7% = **97.1%**)，中位数 **$1.95**、上四分位只有 $3.75，说明商品以低价日用品为主 (如装饰品、文具、厨房小工具)。最右档是 **$20+ (0.2%)**，页面分段里没有">50 美元"这一档，讲的时候按图上标签说。这种低价商品为主的特征符合"在线杂货/礼品店"的商业模式。

#### Revenue (单笔交易收入)

**答辩解释**: Revenue = Quantity × Price，反映每笔交易 (每个明细行) 的实际收入。**分布并不是逐段递减的**——最高峰在中间偏左的 **$10-20 档 (35.8%)**，其后依次是 $0-5 (25.5%)、$5-10 (17.1%)、$20-50 (14.5%)、$50-100 (4.2%)、$100+ (2.9%)；$0-20 三档合计 78.4%。原因是 $0-5 虽然单价低但件数也少，而多数明细行是"几件 × 一两美元"的组合，落到 $10-20 这一档。中位数 $12.5、均值 **$21.6** (= 总收入 864.4 万 / 399,599 行)，右偏主要来自 50 美元以上的长尾批发行。这种偏态正是后续 RFM 分析中需要做对数/排名变换的原因。

### 2.2 热销商品排名 (横向柱状图 + Top-N 滑块)

**图表类型**: Plotly 横向柱状图 (px.bar, color='Revenue')

**交互功能**: 顶部有滑块 (Top-N slider) 控制显示数量，范围 5-30，默认 20。图表高度随 N 值动态调整。

**展示内容**: 按总收入排名的前 N 个商品，悬浮显示销量和订单数

**怎么来的**: `explore_df.groupby(['StockCode', 'Description']).agg(收入=sum(Revenue), 销量=sum(Quantity), 订单数=nunique(Invoice)).sort_values('收入', ascending=False).head(top_n)`

**答辩解释**: 热销商品以家居装饰和节日用品为主。排名靠前的爆款商品 (如 WHITE HANGING HEART T-LIGHT HOLDER — 白色心形烛台架) 收入远超其他商品。这体现了典型的"二八定律"——少量头部商品贡献了大部分收入。拖动滑块可以观察不同 N 值下的排名变化。配合时间筛选器，可以对比不同季度的热销商品差异。

---

## 页面 3: 👥 客户特征分析

> 本页始终使用完整清洗后数据，不受时间筛选影响。

> **页面定位**: 本页承担两个任务 —— (1) 展示经典 RFM 模型及其分布；(2) 在 R/F/M 之外引入**两个非 RFM 的客户级特征**（AOV 平均客单价、品类广度），共 5 个维度刻画客户。这 5 个维度就是第 4 页聚类模型的输入特征集。

### 3.1 RFM 核心指标 (紧凑表格)

**展示内容**: 一张 Markdown 表格，三行分别是 R / F / M，四列是中位数、均值、上四分位、前 10% 阈值。行标签用加粗的 `**R** · 最近购买间隔` 形式，一眼就能对上是哪个维度。

| 维度 | 中位数 | 均值 | 上四分位 | 前10%阈值 |
|:---|:---:|:---:|:---:|:---:|
| **R** · 最近购买间隔 | 距上次购买天数的中位数 | 平均天数 | 上四分位天数 | 前 10% 的天数阈值 |
| **F** · 购买频率 | 订单数中位数 | 平均订单数 | 上四分位订单数 | 前 10% 的订单数阈值 |
| **M** · 消费金额 | 消费金额中位数 | 平均消费金额 | 上四分位金额 | 前 10% 的金额阈值 |

**怎么来的**: RFM 模块 `calculate_rfm()` 将清洗后的交易数据按 `Customer ID` 分组聚合:
- Recency = (快照日期 - 最后购买日).days，快照日期 = max(InvoiceDate) + 1天
- Frequency = nunique(Invoice)，即不重复的发票数
- Monetary = sum(Revenue)，即该客户的总收入

`compute_rfm_segments(rfm_df)` 再对每个维度算出中位数、均值和各分位数，填入表格。

**设计说明**: 早期版本用 6 个独立的 `st.metric` 卡片展示"平均/中位数"，占用空间大且指标不够醒目。改成紧凑表格后，R/F/M 三行对齐、四个统计量并排，既省空间又便于横向对比。这里刻意用"上四分位""前 10% 阈值"等中文表述，避免 P75/P90 这类术语让评委困惑。

**答辩解释**: 均值和中位数的差异揭示了偏态程度。比如 Frequency 均值远大于中位数，说明少量高频客户拉高了均值，大多数客户实际购买次数很少。Monetary 的均值/中位数差异更大，说明消费金额分布更加不均匀。这种偏态是 RFM 分析中必须处理的核心挑战。

### 3.2 R/F/M 分布分段统计图 (3 个并排)

**图表类型**: 3 个并排的业务分段柱状图 (`px.bar`，与页面 2 的方案 C 一致)，柱顶标注每段占比 `%`，柱子颜色由浅到深映射客户数。

**展示内容**: 分别对 Recency / Frequency / Monetary 按业务阈值切分区间，统计每个区间的客户数与占比。相比学术直方图，分段图更能突出"大部分客户落在哪几档"。

**怎么来的**: 同样调用 `compute_rfm_segments(rfm_df)`，取每个维度的 `labels` / `counts` / `pcts` 三组数组绘制。图下方有一行 `st.caption` 解读文字，概括三个维度的分布特征。

#### R - 最近购买间隔分布

**答辩解释**: Recency 分布相对均匀，呈轻微右偏。中位数约 52 天说明大多数客户在 2 个月内有购买行为。超过 200 天未购买的客户 (右侧分段) 基本可判定为流失客户。

#### F - 购买频率分布

**答辩解释**: 严重右偏。绝大多数客户只有 1-5 次购买 (左侧分段占了绝大部分占比)，极少数客户购买数十甚至上百次。这种"长尾"分布在电商数据中非常常见——80% 的购买来自 20% 的客户。

#### M - 消费金额分布

**答辩解释**: 三个维度中偏态最严重的一个。大部分客户总消费在几百美元以下，少量 VIP 客户消费数千甚至上万美元。偏度高达 24+，这是为什么对数变换效果不佳而排名/组合变换更有效的原因。

### 3.3 扩展客户特征 (非 RFM)

**展示内容**: 两个并排的分段统计图 (与 3.2 同款)，分别展示 **AOV (平均客单价)** 与 **N_products (品类广度)** 在各区间的客户数与占比；图下方是一张业务含义表 (计算方式 / 业务含义 / 中位数 / 上四分位)，再配一段随数据动态生成的解读。

**怎么来的**: `calculate_extended_features(cleaned_df)` 按 `Customer ID` 分组算出两列，`compute_extended_segments(rfm_df)` 再按业务区间分段统计 (`$0-100 / $100-200 / … / $1K+` 与 `1-10 / 11-25 / … / 200+ 种`)。

| 特征 | 计算方式 | 业务含义 |
|:---|:---|:---|
| **AOV** (平均客单价) | 客户总收入 / 发票数 | 每次消费的平均金额，衡量单次消费强度 |
| **N_products** (品类广度) | 不重复 StockCode 数 | 买过多少种不同商品，衡量消费宽度 |

> **口径细节**: AOV 用**未取整**的收入相除再取整，与「先给 Monetary 取两位再除以 Frequency」相比可能有 0.01 的差异 (全量 4,295 人中 28 人差 0.01)，属更精确的一侧。

**为什么是这两个特征 (答辩重点)**: 这两个不是随手挑的。我们把 7 个候选特征全部算出来，按「与 R/F/M 的共线性 + 分布可控性」筛了一遍:

| 候选特征 | 与 R | 与 F | 与 M | 偏度 | 零值% | 结论 |
|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **AOV 平均客单价** | -0.05 | 0.10 | 0.33 | 11.6 | 0 | ✅ **采用**: 与 R/F/M 的相关性在候选里最低，是最独立的「单次消费强度」维度 |
| **品类广度** | -0.29 | 0.66 | 0.41 | 6.2 | 0 | ✅ **采用**: 与 F 中等相关但语义正交 (F 是「来几次」，广度是「每次买多宽」) |
| 活跃月数 | -0.43 | 0.70 | 0.37 | 1.7 | 0 | ❌ 与 F 相关 0.70，信息重复 |
| 退货次数 | -0.18 | 0.73 | 0.46 | 9.9 | 60.1 | ❌ 与 F 相关 0.73，且 60% 客户为 0 |
| 客户生命周期跨度 | -0.50 | 0.47 | 0.23 | 0.4 | 34.6 | ❌ 三分之一客户只买过一次，跨度为 0 |
| 单次平均件数 | 0.00 | 0.01 | 0.10 | 45.7 | 0 | ❌ 最大 87,167 件/单，极端重尾难处理 |
| 退货率 | 0.01 | 0.03 | 0.03 | 2.9 | 60.1 | ❌ 定义失真 (退货发票数可超过正向发票数) |

**筛选标准**: 加特征的价值在于**带来 R/F/M 之外的新信息**。若候选与 F 相关 0.7 以上，塞进聚类只是把 F 重复算了一遍，不会让分群变好 —— 所以「与现有维度弱相关」是硬门槛。两个采用的特征彼此相关仅 **0.16**，互不重复。

> **⚠️ 两行「退货」的口径 (被追问时要先说清)**: 退货信息在清洗第 1 步就被整批删掉了，所以这两个候选只能用**原始数据**回算，口径不同结果差别很大。**本表这两行的口径当时没有记录下来**（从"退货次数"偏度 9.9 高于发票级的 8.16 推测，更像是按退货**行数**计数），而这次复核能给出的是**发票级**口径 (该客户 `Quantity < 0` 的发票数、该发票数 / 正向发票数)，实测为: 退货次数 与 R/F/M 相关 **-0.16 / 0.72 / 0.44**、偏度 8.16、零值 60.1%；退货率 相关 **0.42 / -0.29 / -0.13**、偏度 2.49。**两套口径下的结论完全一致** —— 退货次数都因"与 F 高相关 + 六成客户为 0"被否，退货率都因"分子可大于分母 (发票级口径下最大到 5.0，即退货发票数可达正向的 5 倍)、定义失真"被否，所以这条不影响任何采用决策。答辩时若被问到具体相关值，直接引用上面这组**可复现**的发票级数字。

**答辩解释**: 这两个特征回答的是 R/F/M 答不了的问题:
- 同样的消费总额，是「来了很多次但每次花得少」还是「来得不多但每次买得多」→ **AOV**
- 同样的购买次数，是「反复买同几款」还是「横向铺开买很多种」→ **品类广度**

### 3.4 五维特征相关性热力图

**图表类型**: Plotly 热力图 (px.imshow)，**5×5** 矩阵。左侧配文字说明，右侧放热力图 (列宽比 1:2)。

**展示内容**: R / F / M / AOV / 品类广度 五个维度两两之间的 Pearson 相关系数，RdBu_r 色阶 (红=正相关，蓝=负相关)，格内标注数值 (.2f)。

**怎么来的**: `rfm_df[['Recency','Frequency','Monetary','AOV','N_products']].corr()`。

**答辩解释**: 这张图同时支撑两个结论:
1. **F 与 M 呈中高度正相关 (r≈0.65)**——买得多的客户也倾向于花得多，这是聚类中把 F/M 合并为 composite「参与度」维度的理论依据；
2. **两个新特征与 R/F/M 基本独立** (AOV–M≈0.33、品类广度–F≈0.66、AOV–品类广度≈0.16)，这正是选它们作为新增维度的理由。R 与 F/M 相关较低 (R–F≈-0.25、R–M≈-0.12)，说明「购买时效性」是相对独立的维度。

> 页面文案里的相关系数是由 `corr` 实时取值填入的，不写死，避免数据变化后文案与图上数字脱节。

### 3.5 频率 vs 最近购买 气泡散点图 (左图)

**图表类型**: Plotly 气泡散点图 (px.scatter)

**展示内容**: X 轴 = Frequency (购买次数)，Y 轴 = Recency (距上次购买天数)，气泡大小和颜色 = Monetary (消费金额)

**怎么来的**: 每个点代表一个客户，直接使用 RFM 原始值。颜色映射使用 Viridis 色阶 (黄色=高消费，紫色=低消费)。

**答辩解释**: 这张图同时展示三个维度的关系。可以观察到: (1) 高频客户 (右侧) 倾向于更近期购买 (Y 值较低) 且消费更高 (气泡较大/偏黄)；(2) 左上角区域 (低频 + 高 Recency) 集中了大量小气泡，代表低频且已流失的客户群体；(3) 右下角 (高频 + 低 Recency + 高消费) 是我们最重要的核心客户。

### 3.6 频率 vs 消费金额 气泡散点图 (右图)

**图表类型**: Plotly 气泡散点图 (px.scatter)

**展示内容**: X 轴 = Frequency，Y 轴 = Monetary，气泡大小和颜色 = Recency (RdYlGn_r 色阶: 绿色=近期，红色=久未购买)

**怎么来的**: 同上，每个点一个客户。

**答辩解释**: 频率和金额呈中高度正相关 (r≈0.65)——买得多自然花得多。关键观察: (1) 右上角的极端客户消费金额远超其他，是典型的 VIP 大客户；(2) 大量客户聚集在左下角 (低频低消费)，这些是一次性购买的"路人"客户；(3) 绿色点 (近期活跃) 主要集中在中低频率区域，说明新客户多为低频客户。

### 3.7 RFM 评分分群柱状图

**图表类型**: Plotly 竖向柱状图 (px.bar, text='客户数量', Blues 色阶)

**展示内容**: 基于分位数评分的客户分群，各群客户数量。图表下方有一个可折叠的 expander「📋 查看分类依据 (评分规则)」，内含两张表: 一张是 R/F/M 各按分位数分为 1-5 档的评分标准表，一张是"分群 → R 分/F 分/M 分组合 → 含义"的分群规则表，方便评委追问分类逻辑时当场展开说明。

> 注意: expander 内的表格刻意用连字符写区间 (如"1-5 档""1-2")而非波浪号，因为同一个 Markdown 块里出现两个 `~` 会被解析成 ~~删除线~~，把中间文字划掉。

**怎么来的**: 
1. 使用 `add_rfm_scores()` 将 R/F/M 各按分位数分为 1-5 档 (5 = 最优)。三个维度**统一先做 `rank(method='first')` 再等频分箱**，因此每档人数严格均匀 (4,295 人 → 每档约 859 人)。R 也必须走 rank 的原因: Recency 是整数天数、重复值密集，直接 `qcut` 会让分箱边界落在重复值上并被 `duplicates='drop'` 合并，导致档位不均、进而使下游分群阈值偏移。
2. R 分反转: Recency 越低 (越近期) → 分数越高
3. 按组合规则分配分群标签:
   - R≥4 且 F≥4 且 M≥4 → 冠军客户
   - R≥3 且 F≥3 → 忠诚客户
   - R≥4 且 F≤2 → 新客户
   - R≤2 且 F≥3 且 M≥3 → 流失风险
   - R≤2 且 F≤2 且 M≤2 → 流失客户
   - 其他规则 → 潜力客户 / 沉睡客户 / 待开发

**答辩解释**: 这是基于规则的传统 RFM 分群方法，与后续 K-Means 机器学习分群形成对比。规则分群优点是可解释性强 (每个客户的分类逻辑清晰)，缺点是规则阈值人为设定、缺乏数据驱动。实际分布为**忠诚客户 (972 人, 22.6%) 与冠军客户 (908 人, 21.1%) 人数最多**；冠军客户以 21.1% 的人数贡献了 **64.4%** 的收入，人数占比与收入占比之间的差距正是分群差异化运营的价值所在。

> 页面下方的这段解读已改为**由 `seg_counts` 与 `scored['Monetary']` 动态计算**，不再是写死的结论文案。因此调整评分口径或更换数据集后，文字会自动跟着柱状图变化，不会出现"图上是 A、文字说 B"的矛盾。

---

## 页面 4: 🎯 K-Means 聚类

> 本页始终使用完整清洗后数据，不受时间筛选影响。

### 4.1 侧边栏参数控制面板

| 参数 | 默认值 | 作用 |
|------|--------|------|
| K 搜索范围 | 2-10 (可扩至 2-15) | 肘部法则和轮廓系数分析的 K 值范围; k_range_end 的下限**与当前值**都会跟随 k_range_start 动态抬升, 避免出现空搜索区间, 也避免"值低于下限"导致页面报错 |
| Winsorize 截断 | 0.995 | 将 F/M 的 top 0.5% 极端值截断，防止离群点主导聚类 |
| 聚类数量 K | **4** | 最终使用的聚类数, 上限 15 与 K 搜索范围对齐。默认取 4 —— 与页面「第一步」轮廓系数给出的推荐 K 一致 |
| **输入特征集** | RFM + 扩展特征 | 两档可切: `RFM + 扩展特征` (R/F/M + AOV + 品类广度) 与 `仅 RFM`。**每种特征集都自动配对实测最优的特征变换方法**, 因此侧边栏不再单独暴露"变换方法"选项 —— 下拉标签里直接写出配对结果 |

**答辩解释**: 侧边栏只保留必要参数，观众可以实时切换观察聚类效果变化。默认配置是 `RFM + 扩展特征 → PCA 降维 2D` + K=4。

**关于「输入特征集」(为什么不再单独选方法)**: 需求要求"除 RFM 外至少再多 2 个特征"，因此默认使用含两个扩展特征的 5 维输入；保留「仅 RFM」档是为了做**对照实验**，证明新增特征确实参与了分群。而"方法"不再作为独立控件，是因为**每个特征集都有它实测最优的方法**:

| 特征集 | 组合特征 2D | PCA 降维 2D | 本项目选择 |
|:---|:---:|:---:|:---|
| 仅 RFM | **0.4486** | 0.4369 | 组合特征 (高 0.012) |
| RFM + 扩展特征 | 0.3034 | **0.3967** | **PCA (高 0.093)** |

把方法选择收进特征集，信息量少一个维度、演示时也不会出现"选了方法却和特征集不搭"的组合。注意：**不同维度数之间的轮廓系数不可直接比大小**（维度升高会让轮廓系数系统性下降），判断依据应看簇画像里新维度是否在各簇之间被拉开差距。

### 4.2 肘部法则折线图 (左图)

**图表类型**: Plotly 折线图 (go.Scatter, lines+markers)

**展示内容**: X 轴 = 聚类数 K，Y 轴 = WCSS (组内平方和 / Inertia)

**怎么来的**: `find_optimal_k()` 函数对 K=2 到 K=10 依次训练 KMeans，记录每个 K 的 `inertia_` 属性。WCSS = 所有样本到其所属簇中心的距离平方和。

**答辩解释**: 肘部法则是确定最优 K 的经典方法。WCSS 随 K 增大而下降 (K 越大簇越小，内部距离越小)。拐点 (肘部) 处增加 K 的收益开始递减——拐点之前应该增加 K，拐点之后继续增加 K 意义不大。通常拐点在 K=3~5 附近，与轮廓系数分析相互印证。

### 4.3 轮廓系数分析折线图 (右图)

**图表类型**: Plotly 折线图 + 星形标记 (go.Scatter)

**展示内容**: X 轴 = K，Y 轴 = 轮廓系数，⭐ 标记 **K≥3 范围内轮廓系数最高的推荐 K**，而不是全局最高点。曲线画的是**当前特征集自动配对的那个方法**，因此页面上能看到的只有两档: **仅 RFM → 组合特征 2D** 推荐 K=4 (0.449)、K=2 处最高 (0.509)；**RFM + 扩展特征 → PCA 2D (默认)** 推荐 K=4 (0.3967)、K=2 处最高 (**0.4587**)。**两档的绝对值不可横向比较** —— 轮廓系数会随维度升高系统性下降，详见下方两档实测表 (4.4 另有四种方法在同一 K=4 下的完整对照)。

**怎么来的**: 同 `find_optimal_k()` 函数，对每个 K 计算 `silhouette_score(scaled, labels)`。星形标记的位置由 app.py 本地从 `k_values` / `silhouette_scores` 中筛选 K≥3 后取最大值计算 (不依赖 utils 返回的新键，避免 Streamlit Cloud 热更新时旧模块缺键报 KeyError)。

**答辩解释**: 轮廓系数衡量"簇内紧密度 + 簇间分离度"，取值 -1 到 1:
- 接近 1: 样本在自己簇内很紧密，且远离其他簇 → 聚类效果好
- 接近 0: 样本在簇边界上 → 聚类效果一般
- 接近 -1: 样本可能被分到了错误的簇 → 聚类效果差

**关键坑点**: 在偏态极强的 RFM 数据上，轮廓系数**通常在 K=2 处最高** (仅 RFM 时约 0.51，默认的 RFM + 扩展特征 → PCA 约 **0.46**)，因为把客户粗分成"活跃 / 沉睡"两团时簇间分离度最大。K=2 并非"没有业务价值"——它反映了数据本身的双峰结构，对判断客户整体健康度是有意义的粗粒度分群。但对精细化营销 (VIP / 新客 / 流失风险 差异化触达) 而言，K=2 粒度不够。因此星形标记刻意跳过 K=2，只在 K≥3 范围内选最高点作为**面向业务操作**的推荐值。

**K 值选择依据 (图下方 st.info 横幅)**: 轮廓系数在 K=2 处最高，说明数据本身具有显著的"活跃 vs 沉睡"双峰结构，是**最有统计证据的一层分群**。但精细化营销 (差异化触达 / 挽留预算分配) 通常需要更细的粒度：K≥3 后曲线进入平台期，肘部法则拐点也大致落在 4-5 区间。综合两者，**K=4 是当前特征集下轮廓系数最高的选择** (默认的 RFM + 扩展特征 → PCA: K=4 为 **0.3967**，K=3 为 0.3844、K=5 为 0.3825)，故本项目**默认取 K=4**。

**K=4 的四个客户群**: 重要价值客户 / 流失客户 / 沉睡客户 / 新客户 (由 `get_cluster_labels` 自动匹配，两档特征集下标签一致)。

**两档特征集下的实测 K 曲线** (即页面切换「输入特征集」时实际画出的两条线: 仅 RFM → 组合特征 2D、RFM + 扩展特征 → PCA 2D；Winsorize=0.995，random_state=42，n_init=10):

| 特征集 → 方法 | K=2 | K=3 | K=4 | K=5 | K=6 | K≥3 推荐 |
|:---|:---:|:---:|:---:|:---:|:---:|:---:|
| 仅 RFM → 组合特征 (2 维) | 0.509 | 0.445 | **0.449** | 0.420 | 0.428 | K=4 |
| RFM + 扩展特征 → PCA (2 维, 默认) | 0.459 | 0.384 | **0.397** | 0.383 | 0.377 | K=4 |

两档的推荐 K 都落在 **K=4**（K=3 与 K=5 紧随其后），说明 K=4 是数据支持的稳定选择，本项目也据此把默认 K 设为 4。**两档绝对值仍不建议横向比较** —— 输入维度不同 (3 维 vs 5 维)，轮廓系数会随维度升高系统性下降 (见 4.3.1 的证据 3 与 4.4 的判断依据)；真要横向比方法，看 4.4 那张"同一特征集下两方法对照"的表。

### 4.3.1 轮廓系数为什么只有 0.40 左右 (答辩必备)

这是最容易被追问的点。0.40 左右不代表聚类"分错了"，而是**客户价值本身是连续谱，簇与簇天然重叠**。三条可以当场给出的量化证据:

**证据 1: 没有点被分错簇，但簇间高度重叠**。计算每个客户到「本簇中心」与「最近他簇中心」的距离比: **100% 的客户都离本簇中心更近** (一个例外都没有)，说明分群是"正确"的；但该比值的中位数只有 **0.43** —— 也就是点到别的簇中心的距离仅为本簇的 2.3 倍，簇间必然重叠，轮廓系数自然被压住。

**证据 2: 负轮廓点极少**。默认配置下轮廓系数为负 (即疑似分错) 的点**只占 0.7%**，绝大多数点只是"落在簇的边缘"，而不是"被分到了错误的簇"。

**证据 3: 数据没有天然团块**。对 5 维特征空间做 PCA: **前两个主成分累计 82.1% 方差 (第一主成分 62.6%)** —— 数据是一个连续的纺锤形，不存在清晰的"几个团块"，因此任何划分方式都只能是"在连续谱上切几刀"。

**再叠加一层维度效应**: 轮廓系数还会随维度升高系统性下降。**同一种方法、同一个 K=4**，只改输入维度，实测三条阶梯都往下走: 组合特征 2 维 **0.4486** → 4 维 **0.3034**；百分位排名 3 维 **0.3898** → 5 维 **0.2857**；对数变换 3 维 **0.3326** → 5 维 **0.2750**。这是度量本身的特性 (高维欧氏距离趋于均匀)，不是聚类质量下降。讲的时候务必**同一方法内比**，跨方法比会把"方法差异"和"维度差异"混在一起。

**参考基准**: 真实业务数据的聚类轮廓系数通常落在 0.2 ~ 0.5 (> 0.4 算良好，0.25 ~ 0.4 属一般但可用)。本项目默认配置 (RFM + 扩展特征 → PCA + K=4) 为 **0.3967**，已接近"良好"档；同一份特征若改用组合特征处理则掉到 0.3034 (4 维)。若被追问"能不能更高"，还可以回答: 把 K 降到 2 就能拿到 **0.4587** (粗分成"活跃/沉睡"两群，正好跨过 0.4 这条"良好"线)，但那对精细化运营没有意义 —— 这也是页面把 ⭐ 推荐点限制在 K≥3 的原因。

### 4.4 两种特征变换方法对比 (Expander 折叠区域)

**展示内容**: 折叠标题「🔬 两种特征变换方法 — 组合特征 vs PCA」，展开后含核心矛盾说明、两方法思路对比表、**为什么这样配对的实测依据表**、被淘汰方法的说明、组合特征短板与 PCA 解法、共用流程, 以及一张**实时刷新的对比表**。

**怎么来的**: 内容分三部分:

**① 两方法思路对比** (组合特征在含扩展特征时是 4 维、PCA 固定 2 维):

| 方法 | 维度 | 特征构造 | 优点 | 局限 |
|:---|:---:|---------|------|------|
| **组合特征 2D** (配「仅 RFM」) | 2D | `[R_rank, R_rank+F_rank+M_rank]` | 降到 2D 且语义可读: 时效性 × 参与度; **仅 RFM 时轮廓系数最高 (0.4486)** | 两维共享 R_rank, 存在正相关 (≈0.6), **非严格正交** |
| **PCA 降维 2D** (配「RFM + 扩展特征」) | 2D | 对 `[R,F,M,扩展特征]` 的排名取前 2 个主成分 | 两个主成分**严格正交**; 同时承载全部输入特征的信息 (前两维解释 82.1% 方差); **加扩展特征后轮廓系数最高 (0.3967)** | 主成分是特征的线性组合, 语义不如"时效性/参与度"直观 |

**② 配对依据 (实测, `random_state=42`, `n_init=10`, K=4)**:

| 特征集 | 组合特征 2D | PCA 降维 2D | 本项目选择 |
|:---|:---:|:---:|:---|
| 仅 RFM | **0.4486** | 0.4369 | 组合特征 (高 0.012) |
| RFM + 扩展特征 | 0.3034 | **0.3967** | **PCA (高 0.093)** |

**这正是侧边栏不再单独暴露"变换方法"选项的原因** —— 每个特征集都配它在该特征集下轮廓系数最高的方法, 只留一个控件、少一层组合。

**③ 实时对比表**: 调用 `cached_compare_methods(_rfm_df, selected_k, winsorize_pct, extra_features)`, 在当前 K、Winsorize 与输入特征集下跑这两种方法的 K-Means, ✅ 标出当前采用的那个, 数值**随侧边栏实时变化**, 不硬编码。

> **另外两种方法评估过但未采用 (答辩若被问"还试过什么"可以答)**: **百分位排名 3D** (仅 RFM 0.3898 / 加扩展 0.2857)、**对数变换 3D** (0.3326 / 0.2750)。对数变换最差的原因是 F/M 偏度极大 (≈10 / ≈24), 对数压缩不够充分、簇仍重叠; 百分位排名三维语义清晰但保留了 F/M 相关性, K-Means 距离里 F/M 被隐式放大。`utils/clustering.py` 里这两个分支仍保留, 便于复现这组对比。

**方法怎么选 (按实测说, 别说过头)**: 只用 RFM 时组合特征略高 (0.4486 vs 0.4369); 加入两个扩展特征后 **PCA 明显更高 (0.3967 vs 0.3034, 高 0.093)** —— 因为 PCA 把 5 个相关性不高的特征压成 2 个**正交**主成分, 而组合特征在含扩展特征时是 4 维、且 `R_rank` 与 `RFM_composite` 仍然正相关。

**核心矛盾**: K-Means 依赖欧氏距离，对 F 和 M 的极度右偏极其敏感 (F 偏度 ~10, M 偏度 ~24)；同时 F 和 M 呈中高度正相关 (r≈0.65)，在 3D 空间中它们的相似信息会被"重复计入"距离, 降低簇间分离度。

**共用流程 (PCA 多一道)**:
1. Winsorizing: 截断 F/M 的 top 0.5% 极端值
2. 百分位排名: `rank(pct=True)` 压缩到 0-1
3. StandardScaler: Z-score 标准化
4. **(仅 PCA)** `PCA(n_components=2)` 取前两个主成分作为聚类空间 —— 刻意**不再二次标准化**: 主成分按方差降序排列, PC1 承载的信息本就最多, 拉平会丢掉这个权重 (实测不拉平时轮廓系数更高)
5. K-Means: `n_init=10, max_iter=500`

> **关于 `n_init`**: 曾用 50, 后改为 10 (sklearn 默认值)。实测在这份 4295 × 2~5 维、已标准化的平滑特征空间上, 两者的簇划分几乎一致 —— K 扫描的轮廓系数仅第 4 位小数有 ±0.003 的差异、**推荐 K 不变**, 但 K 扫描耗时从 **4.53s 降到约 2.5s**。这是纯收益的调整, 答辩若被问到可以直接给出这组对比数据。

**⚠️ 组合特征的短板与 PCA 的解法 (答辩必备)**: `R_rank` 同时是第 1 维和第 2 维 (`R_rank+F_rank+M_rank`) 的组成部分, 严格讲两维**不正交**, 相当于把 R 在距离中"算了两次"。展开欧氏距离:

```
d² = ΔR_rank² + Δ(R+F+M)² = 2·ΔR² + ΔF² + ΔM² + 2·ΔR·ΔF + 2·ΔR·ΔM
```

即 R 权重约 F/M 的 2 倍, 且带两个正交叉项。这是组合特征**用简单性换可解释性**的自觉取舍。**而这个短板现在有现成解法: 含扩展特征的 5 维输入改用 PCA, 两个主成分严格正交, 轮廓系数还从 0.3034 提到 0.3967**。答辩时若被问"你的两维是不是正交", 标准答法是: 承认组合特征"相关约 0.6、非严格正交", 紧接着说明"所以 5 维输入我们改用 PCA —— 两维严格正交、轮廓系数高 0.093; 保留组合特征是因为它在仅 RFM 时更高、且语义一眼可读" —— 既诚实, 又展示了对方法的完整评估。

**答辩解释**: 这是项目的核心方法学选择。传统方法 (对数变换) 效果最差，因为 F 和 M 偏度极大，对数压缩不够充分。百分位排名法改善了偏态问题但保留了 F/M 的相关性, K-Means 距离里 F/M 被隐式放大。组合特征法把 F 与 M 打包为一个"参与度"标量, 并将聚类空间从 3D 降到 2D。**PCA 是在"降维"这条路上更彻底的一步 —— 同时降维与正交化, 因此含扩展特征时用它。**

**启用扩展特征后的变化 (重要)**: 侧边栏切到「RFM + 扩展特征」时, **组合特征会从 2 维变成 4 维**（两个新特征各追加一维，都以**百分位排名**形式加入，与其他维度同口径；rank 只看次序、天然抗离群，AOV 偏度 11.6 也无需额外截断），而 **PCA 仍固定输出 2 维**。这正是"两档特征集配不同方法"的由来:

1. **组合特征的"降到 2D"优势在 5 维输入下不再成立**，但把 F/M 合并为「参与度」标量的动机不变（F 与 M 仍中高度相关，合并可消除距离中的隐式加权）；**真正一直在 2 维、且把新特征信息也装进去的是 PCA** —— 这是它在加特征后反超的原因。
2. **加入弱相关的新维度会让轮廓系数下降**（实测：用组合特征处理 4 维时 0.3034 vs 仅 RFM 2 维时 0.4486），这是维度升高时的系统性现象，**不同维度数之间的轮廓系数不可直接比大小**。判断加特征是否有价值，要看聚类画像里新维度在各簇之间是否真被拉开了差距 —— 实测 (默认配置 K=4) 各簇的**平均客单价区间为 $170 ~ $599、平均品类广度区间为 15.4 ~ 145.3 种**（见 4.7 画像表），差距分别达 3.5 倍与 9.4 倍，说明这两个特征确实参与了分群，而不是噪声。

### 4.5 聚类结果 KPI (5 个指标)

| 指标 | 含义 |
|------|------|
| 聚类数量 | 当前选择的 K 值 |
| 轮廓系数 | 当前参数下的聚类质量评分 |
| 参与聚类客户 | 参与聚类的客户总数 |
| **输入维度** | 当前配置下实际进入 K-Means 的维度数 (**仅 RFM → 2 维; RFM + 扩展特征 → PCA 固定 2 维**)。**特意把它显式列出来**，避免观众只看轮廓系数却忽略维度数 —— 例如组合特征在仅 RFM 时是 2 维 (0.4486)、若拿去处理 5 维输入会变成 4 维 (0.3034)，两个数字不可直接比较 |
| 变换方法 | 当前使用的特征变换方法，**由「输入特征集」自动配对** (仅 RFM → 组合特征 2D; RFM + 扩展特征 → PCA 降维 2D)，依据是该特征集下轮廓系数更高 |

### 4.6 2D 特征空间散点图 (交互框选)

**图表类型**: Plotly 2D 散点图 (`px.scatter`)，支持 lasso/box 框选

**展示内容**: 每个点 = 一个客户，X/Y = 聚类实际使用的特征空间坐标 (RFM + 扩展特征 → PC1「第一主成分」/ PC2「第二主成分」；仅 RFM → R_rank「时效性」/ RFM_composite「参与度」)，颜色 = 聚类标签。悬浮显示 Customer ID、原始 R/F/M 值。

**方法分支** (取决于当前特征集的维度数):
- **维度数 = 2**: 直接展示这两个特征维度，图标题为"聚类特征空间散点图 (支持框选交互)"。**当前侧边栏的两个特征集都走这条分支** (仅 RFM + 组合特征 = 2 维；RFM + 扩展特征 + PCA = 2 维)。
- **维度数 ≥ 3**: 图上方额外出现一个 `st.selectbox`，让用户在**所有两两组合**中切换切片 (3 维 → 3 条、4 维 → 6 条、5 维 → 10 条)。图标题追加提示 **"{n} 维空间切片 (完整聚类维度: …)"**，明确说明展示的只是高维空间的一个投影切片，避免误以为聚类发生在 2D。
  > 当前侧边栏的两个特征集实际都落到 2 维 (见下)，所以**页面上不会出现这个下拉框**；切片逻辑作为兜底保留 —— 若将来把「RFM + 扩展特征」换回组合特征 (变 4 维)，它会自动出现并给出 `R-时效性 vs RFM-参与度`、`AOV vs 品类广度` 等 6 种组合。
  > 早期版本对多维方法只取前两维 (R, F) 硬编码显示，完全丢失 Monetary 维度，已修复为可切换切片。
  > selectbox 的 `key` 里带了当前特征集，否则从 5 维切到 2 维时旧选中值已不在选项里，会直接报错。

**交互功能**: 使用 Streamlit 的 `selection_mode="points"` + `on_select="rerun"`，用户可以用 lasso (套索) 或 box (矩形) 工具框选散点，框选后页面重新运行并展示选中客户的详细信息表格 (Customer ID, R, F, M)。

**怎么来的**: 使用 `prepare_features()` 返回的 `transformed_df`, 根据 `feature_names` 长度分支处理。**注意**: `transformed_df` 只含工程特征列，不含 Cluster / Customer ID / 原始 R/F/M，且索引是新 RangeIndex。因此需先 `reset_index(drop=True)`，再按**行序**从 `clustered_df` 把这几列补回来 (只补 `transformed_df` 中尚不存在的列，避免覆盖 rank/log 方法已有的同名列)。早期直接访问 `plot_2d['Cluster']` 会 KeyError，就是漏了这步位置合并。

**为什么不用组合特征处理 5 维输入 (答辩必备)**

高维数据投影到 2 个维度时，看到的分离度**必然低于全维空间**。我们评估过"用组合特征直接吃 5 维输入"，实测很差：组合特征在 4 维空间整体只有 **0.3034**，而各个 2D 切片只有 **0.1303 ~ 0.2621**（最好的切片是 `RFM_composite vs AOV` 0.2621；而页面默认会展示的 `R_rank vs RFM_composite` 只有 0.1985）。更麻烦的是 `AOV vs 品类广度` 只有 0.1303 —— 这两个新特征彼此相关性仅 0.158，投影到这两维几乎看不到簇结构。

**这就是含扩展特征时改用 PCA 的原因**: PCA 把 5 个特征压成 **2 个正交主成分**（累计解释 82.1% 方差），全程都在 2 维、没有投影损失，轮廓系数 0.3967，比组合特征的 0.3034 高 0.093。若被追问"为什么加了特征反而要换方法"，答"因为在 5 维输入下组合特征会退化到 4 维且两维不正交，切片后簇边界糊掉；PCA 能在保留全部特征信息的同时维持 2 维正交空间"即可。

**当前配置下的 2D 图 (不会出现切片选择)**: 两档配置实际都是 2 维（仅 RFM + 组合特征 / 扩展 + PCA），所以 2D 图**直接展示这两个维度**，不会出现"请选择展示哪两个维度"的下拉框。切片逻辑仍保留在代码里 —— 一旦将来配置变成 ≥3 维（例如给 PCA 换回组合特征），它会自动出现并在图标题里注明是"n 维空间切片"，避免误以为聚类发生在 2D。

**PCA 配置下页面会额外标出轴的来源**: 主成分是特征的线性组合、不像"时效性/参与度"那样一眼可读，因此 PCA 配置下页面会给出一行说明，直接列出**解释方差与载荷** —— 默认特征集下 PC1 的载荷在 R/F/M/AOV/品类广度 上**全为正**（→ 越往右综合价值越高），PC2 在客单价上 +0.75、在时效性上 -0.54（→ 是一条"高客单低频 vs 低客单近期"的对比轴）。**答辩时用这行说明讲 PCA 的轴，比空口解释主成分要清楚得多**。

**答辩解释**: 2D 图展示的是聚类**实际发生的特征空间**——K-Means 就是在屏幕上这个平面里计算距离、分配簇的（PCA 配置下 PC1/PC2 严格正交，没有信息重叠）。换句话说，**图上看到的簇间距离就是算法真正"看到"的距离**，因此它既是结果展示、也是模型的可解释性出口。框选交互允许观众深入探索: 选中某个区域的客户后，弹出的详情表格展示他们的原始 RFM 值，帮助理解"为什么这些客户被分到了同一个簇"。

> **为什么不再放 3D 散点图 (答辩可能被问)**: 页面早期有过一张「客户 3D 分群可视化」，把聚类结果画在原始 R/F/M 三轴上。实测发现它**在几何上就不适合承担这个任务**：F 与 M 高度相关 (原始 Pearson **0.648**；对数尺度下 `log1p(F)` 与 `log1p(M)` 更达 **0.818**)，等于同一信息画了两遍，而且 F/M 极度右偏，九成客户挤在坐标轴起点角落 (线性刻度下只占平面约 6% 的面积)。改坐标轴也治不好，量化过一轮——原始 R/logF/logM 视角下同一套聚类标签的轮廓系数只有 **0.1553** (仅 RFM 配置 0.2146)，换成「聚类两维 + 金额排名」也才 0.3612，且 3D 图在投影上必然丢掉信息。最终**删除该图**：聚类结果的空间分布由本节的 2D 图 (聚类真实空间) + 4.7 画像表 + 4.10 热力图共同承担，信息更准也更全。

### 4.7 聚类画像表格

**展示内容**: 每个簇的详细统计:

| 列 | 含义 |
|----|------|
| 客户分群 | 中文标签 (如"重要价值客户") |
| 客户数 | 该簇包含的客户数 |
| 客户占比% | 该簇客户数 / 总客户数 × 100 |
| 平均R(天) | 该簇平均 Recency |
| 平均F(次) | 该簇平均 Frequency |
| 平均M($) | 该簇平均 Monetary |
| **平均客单价** | 该簇 AOV 均值 (仅「RFM + 扩展特征」时出现) |
| **品类广度** | 该簇 N_products 均值 (仅「RFM + 扩展特征」时出现) |
| 总收入 | 该簇客户的收入总和 |
| 收入占比% | 该簇收入 / 总收入 × 100 |

**怎么来的**: `clustering.py` 的 `run_kmeans()` 返回 `cluster_profile`，通过 `groupby('Cluster').agg(...)` 计算每个簇的聚合统计。两列扩展特征由 `extra_features` 参数驱动：传入时 `run_kmeans` 会额外聚合 `Avg_AOV` / `Avg_N_products`，页面据此动态插入两列（列名取自 `EXTENDED_FEATURE_INFO`），未启用时不出现。

**答辩解释**: 聚类画像是理解聚类结果的核心表格。通过对比各簇的 R/F/M 均值，可以清晰看到每个群体的特征差异。特别关注"客户占比 vs 收入占比"的反差——默认配置实测: **重要价值客户 25.8% 的人贡献 75.7% 的收入**，沉睡客户 29.8% 的人只贡献 3.2%，帕累托效应直接摆在这张表里 (四个群的完整对照见下方"聚类标签中文对照表")。

**扩展特征列的作用 (默认配置 K=4 下的实测)**: 平均客单价在各簇间从 **$170 到 $599**、平均品类广度从 **15.4 种到 145.3 种**，差距达 3.5 倍与 9.4 倍。这正是"新特征确实参与了分群"的直接证据 —— 如果各簇在这两列上几乎一样，说明它们只是增加了维度噪声，对分群没有贡献 (也是判断"加特征是否有价值"的正确方式，而不是去比不同维度数下的轮廓系数)。

### 4.8 雷达图 (Radar Chart)

**图表类型**: Plotly 极坐标散点图 (go.Scatterpolar)

**展示内容**: 每个簇一个多边形，三个轴分别为 R (已反转: 值越高=越近期), F, M，面积越大=综合价值越高

**怎么来的**: 
1. 对 `cluster_profile` 的 Avg_Recency/Frequency/Monetary 做 Min-Max 归一化到 **[0.15, 1]** (下限 `_RADAR_MIN=0.15`, 避免最差簇在三个维度上都归 0 塌缩到圆心, 从视觉上"消失"; 相对差异仍保留)。当某维度所有簇数值完全相同 (极差 < 1e-10) 时, 归一化退化为 0.5, 显示为规则三角形。
2. Recency 反转: `norm_R = (1 + _RADAR_MIN) - normalized_R`, 使"值越高=越近期=越好", 与 F/M 方向一致; 反转后仍在 [0.15, 1] 区间。
3. 每个簇画一条闭合的极坐标折线, `fill='toself'`, `opacity=0.5`。

**答辩解释**: 雷达图将三维 RFM 特征投射到极坐标上，面积大小直观反映客户综合价值。重要价值客户簇的三角形面积最大 (三个维度都高)，流失客户面积最小 (三个维度都低)。形状差异也很关键——比如"新客户"可能在 R 维度上突出但 F/M 较低，形状偏向 R 轴。这种可视化在答辩中非常直观。

### 4.9 聚类详情卡片

**展示内容**: 每个簇一张卡片，包含:
- 簇名 (C0: 重要价值客户)
- 特征标签 (近期活跃 / 高频购买 / 高消费)
- 客户数 + 占比
- 平均 R/F/M
- **平均客单价 / 平均品类广度** (仅「RFM + 扩展特征」时出现)
- 收入占比
- 簇轮廓系数

**怎么来的**: 标签由 `get_cluster_labels(profile)` 基于 R/F/M 阈值规则生成 (只依赖 R/F/M 均值, 因此新增扩展特征不会改变标签逻辑):
- 比较每个簇的 R/F/M 均值与全体均值
- R < 均值 → "近期活跃"，R ≥ 均值 → "长期未购"
- F > 均值 → "高频购买"，F ≤ 均值 → "低频购买"
- M > 均值 → "高消费"，M ≤ 均值 → "低消费"
- 根据 R/F/M 组合匹配预设的 9 种中文标签 (如 R低+F高+M高 → "重要价值客户")

**答辩解释**: 卡片将技术结果翻译成业务语言。"近期活跃 + 高频购买 + 高消费"比"Cluster 0"更容易被业务人员理解。簇轮廓系数反映该簇内部的紧密程度和与其他簇的分离程度——越高越好，通常核心客户簇的轮廓系数较高。

### 4.10 聚类中心热力图

**图表类型**: Plotly 热力图 (px.imshow)

**展示内容**: 归一化后的聚类中心值，颜色越绿=得分越高

**怎么来的**:
1. 取 `cluster_centers` (K-Means 模型的特征空间中心点)
2. 每列做 Min-Max 归一化到 [0, 1]
3. 列 = **当前特征集实际聚类的那几维**，页面上可达的两种情况: 默认「RFM + 扩展特征 → PCA」是 **PC1 + PC2 (2 列)**；切到「仅 RFM → 组合特征」是 **R_rank + RFM_composite (2 列)**
4. 列名映射 `col_map` 里已登记 `PC1` / `PC2` / `R_rank` / `RFM_composite`，否则会直接显示英文列名
5. `log` 方法对 Recency 做的反转 (`1 - x`，因为原始 Recency 越高=越久没买) 属于**保留但当前 UI 不可达**的分支: 侧边栏已不再单独暴露方法选择，只有 rank/log 方法才会出现 `Recency` 原始列

**答辩解释**: 热力图是最紧凑的聚类结果可视化方式。一行 = 一个簇，一列 = 一个特征维度，颜色深浅一眼就能看出每个簇在各维度上的强弱。绿色集中在一侧说明该簇在该维度上表现优秀。例如，重要价值客户在所有维度上都是深绿色，流失客户在所有维度上都是浅/红色。

**默认配置 (PCA) 下怎么读这张图**: 只有 **PC1 / PC2** 两列 —— PC1 是"综合价值"轴 (五个输入特征的载荷全为正)，PC2 是"客单价 vs 时效"对比轴，因此这张图回答的是"每个簇在两个主成分上的位置"。至于"AOV 与品类广度这两个新特征到底有没有参与分群"，**在默认配置下不看这张热力图，而看 4.7 画像表的 Avg_AOV / Avg_N_products 两列** (实测各簇拉开到 $170~$599 与 15.4~145.3 种) —— 因为原始特征已被压进主成分，中心点里不再存在单独的 AOV 列。若切到「RFM + 扩展特征 + 组合特征」这条非默认路径 (需改代码)，热力图才会直接出现 AOV/品类广度两列。

> **⚠️ 关于"两列是否正交"**: 页面 caption 与本文档均已明确说明——composite 方法的 R_rank 与 RFM_composite ( = R_rank + F_rank + M_rank ) 共享 R 项, 相关系数约 0.6, **非严格正交**。这属于用简单性换可解释性的自觉取舍, 详见 4.4 章节"需要澄清的取舍"部分。若被追问, 应主动承认, 并说明若要严格正交可改用 PCA 降维。

---

## 聚类标签中文对照表

默认配置 (K=4 · RFM + 扩展特征 → PCA) 下的标签分配与其画像:

| 标签 | R 特征 | F 特征 | M 特征 | 客户数 | 收入占比 | 业务含义 |
|------|--------|--------|--------|:---:|:---:|----------|
| **重要价值客户** | 近期活跃 | 高频购买 | 高消费 | 1,110 (25.8%) | **75.7%** | 核心 VIP (同时是高客单价 + 高品类广度)，重点维护 |
| **新客户** | 近期活跃 | 低频购买 | 低消费 | 929 (21.6%) | 9.0% | 近期有购买但还不频繁，需要培养购买习惯 |
| **流失客户** | 长期未购 | 低频购买 | 高消费 | 975 (22.7%) | 12.2% | 曾经消费不低但已长期未购，值得定向挽回 |
| **沉睡客户** | 长期未购 | 低频购买 | 低消费 | 1,281 (29.8%) | 3.2% | 沉睡最久、价值最低，唤醒成本高 |

> **一句话价值**: 重要价值客户只占 **25.8% 的人**却贡献 **75.7% 的收入** —— 这个反差就是差异化运营的依据；而"流失客户"人均消费 ($1,079) 明显高于"沉睡客户" ($213)，说明前者更值得投放挽留预算。

---

## 页面 5: 🔗 关联规则分析

> 本页始终使用完整清洗后数据，不受时间筛选影响。

### 5.1 侧边栏参数控制

| 参数 | 默认值 | 范围 | 作用 |
|------|--------|------|------|
| 最小支持度 (min_support) | 0.02 | **0.005-0.030** | 商品组合至少出现在多少比例的交易中 |
| 最小置信度 (min_confidence) | 0.30 | **0.1-0.7** | 规则 A→B 中，买 A 的客户有多大比例也买了 B |
| 分析商品数 (Top-N) | 100 | 50-300 | 只取购买频次最高的前 N 种商品参与分析 |

> **范围是扫出来的，不是随手定的**: 两个阈值的量程由 5.2 节的网格扫描反推得到。原实现的支持度范围是 0.005–0.10，但实测 **≥0.05 时一条规则都挖不出来**（商品对的最高支持度只有 4.67%），可调区间有一半以上是空区间；置信度 >0.70 时规则数也不足以支撑筛选。现已按扫描结果收窄。

**答辩解释**: 支持度控制"多常见"，置信度控制"多可靠"，Top-N 控制计算量。默认参数下挖出 170 个频繁项集、115 条关联规则，提升度范围 1.70-17.90。降低支持度会得到更多规则但计算变慢；提高置信度会过滤掉弱关联。

### 5.2 阈值范围如何确定 (敏感性扫描)

**展示内容**: 两张图 + 一张结论表。左图是**规则数关于 (支持度 × 置信度) 的热力图**，右图是**规则数随支持度衰减的折线**（每条线一个置信度档，支持度取对数轴更清楚）。下方用表格给出推荐区间及其依据。

**怎么来的**: `threshold_sweep(basket_matrix, supports, confidences, min_lift=1.0)`。优化点在于：**频繁项集对 support 单调**（低阈值下的项集集合必然包含高阈值下的），因此只需在最低支持度上跑一次 fpgrowth，再按 support 过滤 + 逐置信度生成规则，避免在 8×8 共 64 个阈值组合上重复挖掘。

**推荐区间的推导规则**: 以「**规则数仍不少于 10 条**」为可操作下限 —— 与 5.7 规则详情表的滑块下限一致，避免定出一个"理论上能调、实际上选不出规则"的空区间。

**实测扫描结果** (Top-100 商品):

| 支持度 | 0.005 | 0.010 | 0.015 | 0.020 | 0.025 | 0.030 | 0.040 | 0.050 |
|:---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 最大规则数 (跨所有置信度) | 6,571 | 1,113 | 366 | 140 | 58 | 30 | 4 | **0** |
| 频繁项集数 | 1,945 | 490 | 259 | 170 | 129 | 115 | 79 | 44 |

> 两行都是页面上那张扫描表的原值 (表由 `threshold_sweep` 实时算出，不是手填)。**注意两个指标的变化方向不一样**: 频繁项集数从 0.040 到 0.050 只从 79 掉到 44，而规则数直接从 4 掉到 **0** —— 因为剩下的那 44 个项集全是单品集，凑不出"两种商品同时出现"的规则。

**结论**:
- **支持度推荐区间 0.005 ~ 0.030**：上界因为 0.030 以上规则数不足 10 条；下界因为再低则规则数爆炸（6,571 条）且大量退化为长尾偶然组合。
- **置信度推荐区间 0.10 ~ 0.70**：超过 0.70 后规则数不足以支撑筛选。
- **0.050 以上是死区**：一条规则至少要求两种商品同时出现，而本数据集里**商品对**的最高支持度只有 **4.67%**，阈值超过它连最高频的那一对商品都进不了频繁项集。作为对照，最高频**单品**的支持度高达 **19.6%** —— 单品与商品对之间这么大的落差，说明该数据集的商品共现相当分散，这也解释了为什么零售购物篮分析的 `min_support` 通常都取得很低。

**一个反直觉的现象 (答辩加分点)**: 支持度越低规则越多，而且**最高提升度反而更高** —— 扫描中 0.005 档最高 lift 达 **41.4**，而默认 0.02 档为 **17.90**。但低支持度的规则大多只覆盖几十笔交易，属于长尾偶然共现，所以默认取 0.02 而不是一味求多。**这就是"确定阈值范围"要平衡的两端：规则数量、规则强度与统计可靠性。**

### 5.3 KPI 指标卡片 (4 个)

| 指标 | 含义 |
|------|------|
| 分析交易数 | 包含 Top-N 商品的发票数 (默认约 15,446) |
| 分析商品数 | 参与分析的商品种类数 (即 Top-N 值) |
| 频繁项集 | 满足最小支持度的商品组合数量 |
| 关联规则数 | 满足支持度+置信度+提升度≥1 的规则条数 |

### 5.4 规则质量分布散点图

**图表类型**: Plotly 气泡散点图 (px.scatter)

**展示内容**: X 轴 = 支持度，Y 轴 = 置信度，气泡大小和颜色 = 提升度 (Viridis 色阶)。悬浮显示完整规则 (中文商品名)。

**答辩解释**: 右上角的规则同时具有高支持度和高置信度，是最有价值的关联规则。颜色越亮 (提升度越高) 表示关联越强——提升度 > 1 说明两商品的出现不是偶然的，而是正相关。

### 5.5 商品关联网络图

**图表类型**: Plotly 散点图模拟网络 (networkx spring_layout 计算坐标)

**展示内容**: 每个节点 = 一种商品 (中文标签)，节点大小 = 该商品参与的关联规则数量 (度)，连线 = 两者之间存在关联规则。悬浮连线显示该规则的提升度与置信度。

> ⚠️ **呈现方式**: 连线用 `go.Scatter(mode='lines')` 绘制，**没有箭头，是无向展示**。这是刻意的——强关联通常互为前项与后项 (如粉色餐具套装→蓝色餐具套装 与 反向 的 lift 同为 17.90)，画箭头会得到两条完全重叠、方向相反的线。逐条规则的方向请以散点图与下方详情表的「前项 → 后项」为准。

**怎么来的**: `build_network_graph(rules_df, top_n=30)` 取提升度最高的 30 条规则构建有向图 (networkx DiGraph 内部保留方向与指标)，用 spring_layout 计算节点位置，再用 go.Scatter 分别绘制边 (lines) 和节点 (markers+text)。

**答辩解释**: 网络图能一眼识别出"枢纽商品" (节点大、连线多) 和"商品簇" (互相紧密关联的群组)。例如三色迷你圆点餐具套装形成一个三角形强关联簇，超大购物袋系列形成另一个簇。这些簇内的商品适合捆绑促销或相邻陈列。

### 5.6 商品共现热力图 (Top 20)

**图表类型**: Plotly 热力图 (px.imshow，YlOrRd 色阶)

**展示内容**: 20×20 矩阵，行商品出现时列商品同时出现的条件概率 P(列|行)。对角线始终为 1。

**怎么来的**: `compute_cooccurrence_matrix(basket, top_n=20)` 取频次最高的 20 种商品，计算 `sub.T @ sub` 得到共现计数，再除以行商品自身出现次数得到条件概率。

**答辩解释**: 颜色越深表示行商品出现时列商品也出现的概率越高。非对角线的高值区域揭示了强共购模式，可用于推荐系统和捆绑促销策略。与网络图互补——热力图展示"全局共现强度"，网络图展示"规则的整体结构与枢纽商品"。

### 5.7 关联规则详情表格

**展示内容**: 按提升度降序排列的规则表，列为: 规则 (中文)、前项 (买了这个)、后项 (也买了这个)、支持度、置信度、提升度。顶部有滑块控制显示条数 (10-100)；**若当前参数下规则不足 10 条，则不渲染滑块，直接全部显示** (滑块下限 10 高于规则总数会导致控件报错)。

**中文翻译**: 页面内置了 52 个高频商品的中文翻译字典 (`PRODUCT_CN`)，散点图悬浮、网络图节点、热力图坐标轴、规则表格统一显示中文商品名 (如 "WHITE HANGING HEART T-LIGHT HOLDER" → "白色悬挂心形烛台")。

**指标含义说明 (Expander)**:
- **支持度**: P(A∪B) = 同时包含 A 和 B 的交易数 / 总交易数，衡量规则的普遍性
- **置信度**: P(B|A) = 同时包含 A 和 B 的交易数 / 包含 A 的交易数，衡量规则的可靠性
- **提升度**: Lift = Confidence / P(B)，> 1 正相关，= 1 无关，< 1 负相关。**答辩重点**: 提升度是衡量关联规则是否有意义的最核心指标

**答辩解释**: 默认参数下 Top 规则全部可以直觉解释——同系列不同色商品互相强关联 (三色餐具套装 lift≈14-18)、配套家具 (白色木柜 lift≈14)、同材质首饰盒 (lift≈13)、互补游戏品类 (lift≈11)。这些规律对实际业务有直接指导意义: 捆绑促销、货架陈列优化、推荐系统。

### 5.8 基于关联规则的营销方案

**展示内容**: 一张按提升度降序排列的**营销动作表**，列为: 前项 (买了这个)、后项 (也买了这个)、支持度、置信度、提升度、**关联力度**、**建议动作**。顶部有 selectbox 控制生成条数 (5 / 10 / 15 / 20)，表下方是一段分档说明与落地提醒。

**怎么来的**: `build_marketing_actions(rules_df, top_n)` 把规则按提升度分档翻译成动作:

| 分档 | 判据 | 建议动作 |
|:---|:---|:---|
| 强 | lift ≥ 10 | 组合成套装 / 配套商品主推 |
| 中 | 5 ≤ lift < 10 | 货架相邻陈列 / 详情页同页推荐 |
| 弱 | lift < 5 | 凑单推荐位 / 满额赠品候选 |

**为什么用 lift 而不是 confidence 分档**: 置信度会被后项自身的高频次抬高（热门商品天然容易"被一起买"），lift 才是"比随机共现强多少倍"的强度指标，直接对应"捆绑的把握有多大"。

**去重处理**: A→B 与 B→A 是同一个商品组合（实测两向 lift 相同，如粉色餐具套装↔蓝色餐具套装均为 17.90），若不去重，同一个套装会在方案表里出现两次。`build_marketing_actions` 按「前项 ∪ 后项」的商品集合去重，保证**一个商品组合只出一条方案**。

**答辩解释**: 这一步把统计结果落成业务动作，是"关联规则分析"的最终产出。默认参数下前 10 个组合里绝大多数是强关联 (lift ≥ 10)，可直接作为捆绑销售的候选清单，例如「粉色 + 蓝色 + 红色三件套迷你圆点餐具」可以做成三色套装主推。页面同时给出了两点落地提醒：一是表里是**商品名**，执行时要换成 SKU 并按库存与毛利复核套装定价；二是关联规则只说明「一起买」不区分因果——像三色餐具这类顾客本来就打算成套买的组合，做套装是顺势而为，而**更有商业价值的是那些顾客自己未必意识到可以搭配**的组合。

---

## 页面 6: 📦 商品与国家调查

> 本页始终使用完整清洗后数据，不受时间筛选影响。

> **页面定位**: 针对 **商品描述 (Description)** 与 **用户所在国家 (Country)** 两个特征做业务探索。两者都是"看起来平常、实际不好用"的字段 —— Description 是文本无法直接统计，Country 分布高度集中 (英国占 91% 的行、84.7% 的收入) 方差太小。本页的任务就是把它们变成可用于业务的维度。

### 6.1 商品描述 (Description) 分析

**展示内容**: 左侧是各品类词的收入贡献横向柱状图 (Y 轴标签为「英文词 · 中文含义」，颜色 = 平均单价)，右侧是「销量 vs 平均单价」散点 (气泡大小 = 收入，X 轴取对数，悬浮显示中文含义)；下方是数据表 (品类词 / 含义 / 词类 / 商品数 / 收入 / 收入占比 / 销量 / 平均单价)，顶部滑块控制显示的品类词数量 (8-25)；再下面是一个可展开的「📖 品类词对照表」。

**怎么来的**: 文本字段无法直接进模型，`description_word_stats(df, top_n)` 的处理链路是:

1. 先把交易聚合到**商品粒度** (`StockCode` + `Description`)，避免热销商品把某个词的频次刷爆；
2. 用正则 `[A-Z][A-Z'\-]{2,}` 从描述里提取候选词 (全大写、长度 ≥ 3)；
3. 过滤功能词/泛化词 (PACK、SET、WITH、AND、DESIGN 等)，保留 BAG / HOLDER / CABINET 这类真正指向品类的词；
4. 汇总每个词的**商品数、收入、收入占比、销量、平均单价**，按收入降序返回；
5. 关联 `WORD_CN` 词典补上**中文含义**与**词类**两列，并拼出「显示名」供图表标签使用。

**中文注释 (`WORD_CN`)**: 屏幕上只有英文品类词对观众不友好，因此为高频词建了一份词典 (共 30 个词，覆盖滑块最大 25 个词的完整范围)，字段为 `cn` (中文含义) 与 `type` (词类)。**注释是逐词核对真实商品名后填写的，不是照词义猜的**，例如：

| 词 | 含义 | 词类 | 核对依据 (该词收入最高的商品) |
|:---|:---|:---|:---|
| RED | 红色系列 | 颜色 | RED HANGING HEART T-LIGHT HOLDER |
| HOLDER | 托架类 (烛台 / 卡片架) | 品类 | CREAM HEART CARD HOLDER |
| T-LIGHT | 茶蜡托 / 小蜡烛 | 品类 | ROTATING SILVER ANGELS T-LIGHT HLDR |
| HOT / WATER / BOTTLE | 热水袋 (来自 HOT WATER BOTTLE) | 品类 | CHOCOLATE HOT WATER BOTTLE |
| UNION | 英国国旗图案 (Union Jack) | 图案 | DOOR MAT UNION FLAG |
| PAPER | 纸质品 (拉花 / 纸链) | 材质 | PAPER CHAIN KIT 50'S CHRISTMAS |

页面上的「📖 品类词对照表」展开区列出全部 30 个词的注释，并说明词类划分的含义 (颜色/图案/风格 = 系列或款式词；品类/材质/形态 = 商品属性词；规格/主题 = 规格与节庆词)。

> **口径说明 (必读)**: 一个商品可能同时命中多个词 (如 `JUMBO BAG PINK WITH WHITE SPOTS` 含 JUMBO / BAG / PINK / SPOTS)，因此**各词的收入合计互相重叠，不能相加当作总量**。最典型的是 `HOT` / `WATER` / `BOTTLE` 三个词其实来自同一个商品名 `HOT WATER BOTTLE` (热水袋)，`RETRO` / `SPOT` / `RETROSPOT` 也高度重叠 —— 这也是为什么要给每个词标注含义，而不是只看词本身。另外描述字段的缺失记录 (2,928 条, 0.56%) 已在清洗阶段移除。

**答辩解释**: 这张表把"商品描述"这个非结构化字段转成了可统计的品类维度。收入贡献最高的是 **RED** (红色系列，覆盖 271 种商品、收入 $918,253、占全站 **10.62%**)，其后依次是 BAG / HEART / WHITE / PINK / HOLDER —— 都是居家装饰与节日小商品的高频描述词，构成店铺的基本盘。

**词类构成 (页面动态计算)**: 默认 15 个词里 品类 5 / 颜色 3 / 形态 2 / 图案 2 / 风格 1 / 材质 1 / 规格 1；拉到 25 个词时 品类 7 / 颜色 5 / 图案 3 / 材质 3 / 形态 2 / 风格 2 / 规格 2 / 主题 1。**数量上品类词最多，但颜色词的收入排名明显更高 —— 前五名里就占了 3 席 (RED / WHITE / PINK)**，说明顾客高度按「同一个商品的不同花色」选购。

> **一个容易被误读的点**: 各词之间的**平均单价差异其实很小** (默认 Top-15 落在 $1.26 ~ $2.15 区间)，所以不能把"平均单价高"理解成"高客单价品类"。该店的品类差异主要体现在**销量规模**上，右侧散点用对数 X 轴才看得清：右下角是"走量"型 (如 RED / PINK 这类覆盖商品多、总销量大的颜色系列词)，相对左上角是"量小价略高"的词 (如 VINTAGE)。

这个结论与第 5 页的关联规则互相印证：关联最强的正是**同系列不同色的低单价小商品** (三色迷你圆点餐具套装 lift≈18)，顾客容易顺手搭配购买，因此描述词里的颜色/系列词既贡献收入、也贡献关联强度。

### 6.2 用户所在国家 (Country) 分析

**展示内容**: 三个层次 —— (1) 4 个 KPI 卡片 (覆盖国家数、英国收入占比、海外客户数、海外平均客单价)；(2) **本土 vs 海外客户画像对比表** + 「客户占比 vs 收入占比」对比柱状图；(3) 国家明细表 (按收入排序，可选择显示 10/15/20/30 个) 与收入 Top 5 国家的月度收入构成面积图。

**怎么来的**:
- `country_summary(df)`: 按国家汇总客户数、收入、收入占比、订单数、客单价、**复购率** (该国内发票数 > 1 的客户占比，用来区分"一次性买家聚集地"与"稳定复购市场")、商品种类数。
- `uk_vs_overseas_profile(df, rfm_df)`: 按客户所属国家二分为「英国本土 / 海外」，对比两组的 R/F/M + AOV + 品类广度均值与收入占比。
- `country_monthly_revenue(df, top_n=5)`: 收入 Top 5 国家的月度收入透视表，供堆叠面积图观察海外份额变化。

> **⚠️ 「客户数」这一列不能求和 (页面已加 caption)**: `country_summary` 计的是**每个国家内部的唯一客户数**，同一客户如果在两个国家都下过单，会在两国各计一次。本数据集有 **5 位**这样的客户 (12417 / 12422 / 12423 / 12431 / 12745)，所以整列合计是 **4,300** 而非全站的 **4,295**。需要"客户总数"请用上方 `uk_vs_overseas_profile` 那张表——它按客户的**首个**国家二分，每人只计一次 (3,955 + 340 = 4,295 ✓)。两种口径都对，只是回答的问题不同: 按国家看规模用前者，按客户看归属用后者。

**为什么 Country 不做聚类特征 (答辩重点)**: 英国占 91% 的行、84.7% 的收入，这个字段的方差太小，塞进 K-Means 几乎不产生区分度——把它当聚类特征，等于给模型加了一个近似常数的维度。但它作为**分析维度**很有价值，所以本页用「本土 vs 海外」的二分对比来挖掘它。

**答辩解释**: 只看收入占比会漏掉一个事实 —— **海外客户人少、单体价值却明显更高**: 海外 340 人 (7.9% 客户) 贡献了 15.3% 的收入，平均消费 $3,880 (英国 $1,852)、平均客单价 $662 (英国 $348)，约为本土客户的 1.9~2.1 倍。这与国家维度的另一个特点吻合：**海外多为批发型小客户**，人少但单笔金额大。对应到运营上，本土适合做频次与复购，海外更适合做客单价与批发阶梯价。国家明细表里的复购率列也能看出差别 (德国 70.1%、法国 76.1% 高于英国 67.1%)。

---

## 性能优化与部署

**部署地址**: Streamlit Community Cloud，仓库 `fkdnyhg123/online-retail-customer-segmentation`，在线地址见文档开头。

大屏数据量近 40 万行，且聚类页涉及 K-Means 反复拟合，为保证本地和云端流畅，做了以下几项优化:

1. **预生成 Parquet 缓存并提交到 Git**: 原始 43MB 的 xlsx 解析很慢，尤其云端每次冷启动都要重新读 Excel。因此把清洗前/清洗后的数据分别落地为 `.cache_raw.parquet` / `.cache_cleaned.parquet` (约 3MB) 并提交仓库。应用启动时优先读 Parquet，冷启动从"解析 Excel"降为"读 Parquet"，速度大幅提升。Parquet 是无损列式存储，不会改变数据内容，只是加载更快。

   **缓存失效机制**: 只判断 Parquet 是否存在是不够的——缓存文件已经提交进仓库，日后一旦改了 `clean_data()` 或换了数据源，应用会静默继续用过期的 Parquet，页面数字与代码就对不上了。为此额外维护一个 `.cache_meta.json` 侧车文件，存放 `xlsx + utils/data_cleaner.py` 两者的 SHA-256 内容指纹。启动时重算指纹: 与侧车一致才读 Parquet，不一致就丢掉缓存重新解析 Excel 并回写三个文件。这里刻意用内容哈希而不是文件修改时间 (mtime)——Streamlit Cloud 每次检出仓库都会重写所有文件的时间戳，mtime 比较在云端结论不可靠。实测: 命中缓存冷启动 3.6s，指纹失配重建 25.5s，且重建结果与原缓存逐单元格完全一致 (`DataFrame.equals` 为 True)。

2. **`@st.cache_data` 包裹重计算**: 特征工程 + 肘部/轮廓搜索封装为 `cached_prepare_and_elbow(...)`，K-Means 拟合封装为 `cached_run_kmeans(...)`。相同参数命中缓存直接返回，避免每次交互都重新拟合模型。

   另有一项低成本调整: 把 K-Means 的 `n_init` 从 50 降到 10 (sklearn 默认值)。实测簇划分几乎不变 (轮廓系数仅第 4 位小数有 ±0.003 差异、推荐 K 不变, 见 4.4)，但单次 K 扫描从 **4.53s 降到约 2.5s**。当前状态的实测耗时 (Streamlit AppTest 同一进程内计时，磁盘数据缓存已命中，连跑 3 次取区间):

   | 操作 | 优化前 (n_init=50, 无 PCA) | 当前实测 (3 次区间) |
   |:---|---:|---:|
   | 冷启动 (默认页 数据概览) | — | 3.6 ~ 3.9s |
   | 首次进聚类页 | 8.07s | **5.1 ~ 5.6s** |
   | 切换输入特征集 (该组合首次计算) | 7.57s | **5.3 ~ 5.6s** |
   | 拖动「聚类数量 K」(4→6) | 3.21s | **2.37 ~ 2.38s** |
   | 缓存命中 (重复参数 / 切走再切回) | 0.37s | **0.31 ~ 0.40s** |
   | 拖动 Winsorize 截断比例 | — | 约 5.6s (截断值变了要全量重算) |
   | 其余页面首次进入 | — | 0.46 ~ 3.33s (最重是关联规则页 3.2 ~ 3.3s) |

   > **计时口径 (答辩时要说清)**: 这是 **AppTest 的后端重跑耗时，不含浏览器渲染 Plotly 的时间**。所以"删掉 3D 分群图"这类纯前端减负不会体现在这张表里——它降的是浏览器端的渲染负担，不是这里的秒数。3 次实测的波动约 ±0.3s。

   拖 K 仍有约 2.4s 的原因: 「方法对比表」的缓存键里带着当前 K，每次拖 K 都要把当前特征集下的两种方法在新 K 下重算 (这部分占大头)，加上聚类本身的重算。**可选的进一步优化**: 把该表固定在某个 K (如 K=4) 下比较方法，拖 K 时就只剩聚类本身的重算 —— 预计能降一半以上 (未实测)。本次未做, 先记录在此。

3. **图例点选切换客户群 (纯前端)**: 2D 特征空间图 (px.scatter 按聚类标签着色 → 每个群一条独立 trace) 用 Plotly 图例控制各群显隐，切换是浏览器端行为，**不触发 Streamlit 重跑后端**，因此加减客户群是瞬时的。(页面早期那张 3D 分群图也用过同样的机制，该图已按 4.6 节末尾的说明删除。)

4. **阈值扫描只挖一次 (第 5 页)**: 敏感性分析要对 8 个支持度 × 8 个置信度共 64 个组合给出规则数。若每个组合都跑一次 FP-Growth，开销无法接受；实现上利用**频繁项集对 support 单调**这一性质，只在最低支持度 (0.005) 上跑一次 `fpgrowth`，之后按 support 过滤 + 逐置信度生成规则。结果整体包在 `@st.cache_data` 里，缓存键为分析商品数，拖动阈值滑块不会触发重扫。

5. **缓存范围的界定 (新增计算为何不用再加磁盘缓存)**: `.cache_meta.json` 的指纹**只覆盖 `xlsx` + `utils/data_cleaner.py`**，这是刻意的 —— Parquet 里存的是"原始数据"与"清洗后数据"，其内容只由这两者决定；`rfm.py` / `clustering.py` / `association.py` / `product_country.py` 都是**读完 Parquet 之后再计算**的模块，改动它们不需要重建缓存，重新算一遍即可。反过来说，**不要把 `rfm.py` 之类加进指纹**，否则改一行 RFM 逻辑就会白白触发一次约 25 秒的 Excel 重解析。

   本次新增的各页计算实测都很轻 (基于 399,599 行清洗数据): 描述词统计 **0.08s**、国家汇总 **0.10s**、本土 vs 海外 **0.05s**、国家月度收入 **0.10s**、阈值扫描 **0.62s** —— 全部已挂 `@st.cache_data`，因此没有再新增磁盘缓存的必要。

   ⚠️ **一处必须留意的缓存键**: 聚类相关的缓存 (`cached_prepare_and_elbow` / `cached_run_kmeans` / `cached_compare_methods`) 的键里**必须包含 `extra_features`**，否则在两档「输入特征集」之间切换时会命中另一档的脏缓存，出现"切了特征集但轮廓系数和画像没变"的假象。

> Streamlit Cloud 提示 "Your app has been throttled" 是社区版对长时间占用资源的应用做的限流保护，属于平台机制而非代码错误，等待或减少并发即可恢复。

---

## 交互功能总览

| 页面 | 交互功能 | 操作方式 | 效果 |
|------|----------|----------|------|
| 数据探索 | 时间范围筛选 | 展开 expander，选择起始/结束月 | 分段统计图和热销排名联动更新 |
| 数据探索 | Top-N 滑块 | 拖动滑块 (5-30) | 热销商品排名动态增减 |
| K-Means | 聚类参数调节 | 侧边栏切换 K/Winsorize/输入特征集 | 所有聚类图表实时更新 (命中缓存则秒回) |
| K-Means | **输入特征集切换 (含自动配对方法)** | 侧边栏 selectbox: `RFM + 扩展特征 → PCA` / `仅 RFM → 组合特征` | 维度数与变换方法一起切换，可对照加特征前后的聚类质量与簇画像 |
| K-Means | 2D 图例点选切群 | 点击图例色块 | 显示/隐藏对应客户群，纯前端瞬时切换 |
| K-Means | 2D 散点框选 | lasso/box 工具选中散点 | 弹出详情表格展示选中客户 |
| K-Means | 2D 切片切换 (仅在配置 ≥3 维时出现) | 图上方 selectbox 选任意两个维度组合 | 当前两档配置都是 2 维，因此不会出现；切片逻辑保留待 ≥3 维配置 |
| 关联规则 | 支持度/置信度/Top-N 调节 | 侧边栏三个滑块 | 重新挖掘规则，所有图表联动更新 (命中缓存则秒回) |
| 关联规则 | **阈值敏感性扫描** | 打开页面即自动计算 (无需操作) | 热力图 + 折线展示规则数随阈值变化, 并给出推荐区间 |
| 关联规则 | 规则数量滑块 | 页面内滑块 (10-100; 规则不足 10 条时自动隐藏) | 控制详情表格显示条数 |
| 关联规则 | **营销方案条数** | 页面内 selectbox (5/10/15/20) | 控制营销动作表生成的组合数量 |
| 关联规则 | 散点图悬浮 | 鼠标悬浮气泡 | 显示完整中文规则名 + 三项指标 |
| 关联规则 | 网络图悬浮 | 鼠标悬浮节点/连线 | 显示商品中文名的关联数/提升度/置信度 |
| 商品与国家调查 | **品类词数量滑块** | 拖动滑块 (8-25) | 控制描述分析显示多少个品类词 |
| 商品与国家调查 | **品类词对照表** | 展开 expander | 查看全部 30 个品类词的中文含义与词类划分 |
| 商品与国家调查 | **国家明细显示数量** | selectbox (10/15/20/30) | 控制国家明细表的行数 |

---

## 答辩常见问题预判

**Q: 为什么选 K=4? (为什么不是经典的 RFM 五类?)**
A: 两层理由。**统计上**: 在 K≥3 的范围里轮廓系数最高的就是 K=4 —— **默认配置 (RFM + 扩展特征 → PCA) 的 K 曲线是 K=3 0.3844 / K=4 0.3967 / K=5 0.3825**，仅 RFM 时也是 K=4 最高 (0.4486，K=5 为 0.4195)，肘部法则的拐点同样落在 4-5 附近。**业务上**: K=4 分出来的四类正好对应「VIP / 新客 / 流失 / 沉睡」——重要价值客户仅占 **25.8% 的人却贡献 75.7% 的收入**，新客户 21.6% 人/9.0% 收入，流失客户 22.7% 人/12.2% 收入，沉睡客户 29.8% 人/3.2% 收入，四类各自对应不同的运营动作 (且"流失客户"人均消费 $1,079 远高于"沉睡客户" $213，挽留预算该投给前者)。之所以不是经典的"五类 RFM 分群"，是因为**这份数据的 K=5 只会把中间态客户切成两个边界模糊的簇** (K=4 为 0.3967、K=5 为 0.3825)，少一个簇反而换来更清晰的分群边界 —— 如果评委追问"为什么不做五类"，直接引用这组对比即可。另外页面并未写死 K，滑块可以随时调到 5 现场验证。

**Q: 轮廓系数算高吗?**
A: 学术基准: < 0.25 差，0.25-0.4 一般但可用，0.4-0.6 良好。**默认配置 (RFM + 扩展特征 → PCA + K=4) 是 0.3967**，已接近"良好"档；作为对照，同一份特征若改用组合特征处理会掉到 0.3034、仅 RFM 的组合特征为 0.4486。而且这份数据本身就是**连续谱**，有三条可当场给出的量化证据 (详见 4.3.1): (1) **100% 的客户都离本簇中心更近** (没有错分)，但"到最近他簇的距离 / 到本簇的距离"中位数只有 **2.3 倍** (距离比 0.43)，说明簇间天然重叠；(2) **负轮廓点只占 0.7%**；(3) **PCA 前两主成分累计 82.1% 方差**，数据是连续纺锤形而非几个团块。所以 0.40 是这份数据的本质属性，不是算法问题。若被追问"能不能更高": 能——默认配置下 K 降到 2 就有 **0.4587** (跨过 0.4 进"良好"档)，但那只是粗分「活跃/沉睡」，对精细化运营没有意义。

**Q: 为什么只保留两种特征变换方法? 怎么配对的?**
A: 因为**每个特征集都有它实测最优的方法**，两者一一配对后就不需要再让用户单独选方法了:

| 特征集 | 组合特征 2D | PCA 降维 2D | 采用 |
|:---|:---:|:---:|:---|
| 仅 RFM | **0.4486** | 0.4369 | 组合特征 |
| RFM + 扩展特征 | 0.3034 | **0.3967** | **PCA** |

配对依据就是上表 (K=4 实测)。**为什么 5 维输入不用组合特征?** 因为组合特征吃 5 维输入时会变成 4 维、且两维仍不正交 (`R_rank` 与 `RFM_composite` 相关约 0.6)，整体只有 0.3034、任一 2D 切片更低 (0.13~0.26)；而 **PCA 把 5 个特征压成 2 个严格正交的主成分** (累计解释 82.1% 方差)，全程 2 维、轮廓系数 0.3967。另外两种方法也评估过但未采用: 百分位排名 (0.3898 / 0.2857) 与对数变换 (0.3326 / 0.2750) —— 对数变换最差是因为 F/M 偏度极大、对数压缩不足；百分位排名语义清晰但保留了 F/M 相关性。**这样收口的好处**: 侧边栏少一个控件，演示时也不会出现"方法选了但和特征集不搭"的组合。

**Q: 除了 RFM，为什么还要加这两个特征? 怎么选的?**
A: 需求要求"除 RFM 外至少再加 2 个特征"。我们没有随手挑，而是把 7 个候选特征全部算出来按两条标准筛选: **(1) 与 R/F/M 弱相关**（否则只是把已有维度重复算一遍）；**(2) 分布可控**（不能有大量结构性零值或极端重尾）。最终采用 **AOV 平均客单价** (与 R/F/M 相关性最高也只有 0.33，是最独立的"单次消费强度"维度) 与 **品类广度**（与 F 相关 0.66 但语义正交: F 是"来几次"、广度是"每次买多宽"），两者彼此相关仅 0.16。被否决的包括: 活跃月数 (与 F 相关 0.70)、退货次数 (0.73 且 60% 客户为 0)、客户生命周期跨度 (34.6% 结构性零值)、单次平均件数 (偏度 45.7)、退货率 (定义失真)。完整对比表见 3.3 节。

**Q: 加了特征后轮廓系数反而下降，怎么解释?**
A: 这是**维度升高导致的系统性现象**，不是聚类变差。轮廓系数基于欧氏距离，维度越多，样本在高维空间里越"分散"，取值普遍下降 —— 所以组合特征 4 维的 0.3034 与仅 RFM 2 维的 0.4486 不能直接比大小。判断"加特征有没有价值"的正确方式是看**簇画像**: 加入 AOV 与品类广度后 (K=4)，各簇的平均客单价拉开到 **$170 ~ $599** (3.5 倍)、平均品类广度拉开到 **15.4 ~ 145.3 种** (9.4 倍)，说明这两个特征确实参与了分群；如果各簇在这两列上几乎一样，才说明它们只是噪声。**而且我们并没有在数值上让步**: 5 维输入改用 PCA 后轮廓系数是 **0.3967**，比组合特征的 0.3034 高 0.093 —— 既用上了全部 5 个特征，又把"维度升高"的损失绕开了。页面上的「输入特征集」切换就是为这个对照实验准备的。

**Q: 支持度 / 置信度的范围是怎么确定的?**
A: 不是随手定的，是**网格扫描**出来的。第 5 页会把支持度 × 置信度共 64 个组合的规则数扫一遍并画成热力图/折线，再以「**规则数仍不少于 10 条**」为可操作下限反推区间: 支持度 **0.005~0.030**、置信度 **0.10~0.70**。关键发现是**支持度 ≥0.05 是死区** —— 一条规则至少要求两种商品共现，而本数据集里**商品对**的最高支持度只有 **4.67%** (最高频**单品**则有 19.6%)，阈值超过它一条规则都挖不出来。原实现把上界设到 0.10，可调区间一半以上是空的，现已按扫描结果收窄。

**Q: 国家为什么不做聚类特征?**
A: 因为方差太小 —— 英国占 91% 的行、84.7% 的收入，把它塞进 K-Means 等于加了一个近似常数的维度，几乎不产生区分度。但它作为**分析维度**很有价值: 把客户二分成「本土 vs 海外」后差异很明显 —— 海外仅 340 人 (7.9%) 却贡献 15.3% 收入，平均消费 $3,880 vs 本土 $1,852、平均客单价 $662 vs $348，说明海外是"人少但单体价值高"的批发型市场。这部分放在第 6 页。

**Q: 商品描述是文本字段，怎么处理和分析?**
A: 分两步: 清洗阶段先移除描述缺失的记录 (2,928 条, 0.56%)；分析阶段在**商品粒度**上用正则提取描述里的高频词 (过滤 PACK/SET/WITH 等功能词)，把商品自动归到品类/系列上，再统计每个词的收入、销量、商品数与平均单价。因为屏幕上只有英文词对观众不友好，页面还为 30 个高频词配了**中文含义与词类注释** (`WORD_CN` 词典)，注释是逐词取该词收入最高的商品名核对后写的，并提供了可展开的完整对照表。一个必须说明的口径是: **一个商品会命中多个词，所以各词合计互相重叠、不能相加** (最典型的是 `HOT` / `WATER` / `BOTTLE` 三个词其实来自同一个商品名 `HOT WATER BOTTLE`)。这个分析也和第 5 页呼应 —— 关联最强的正是同系列不同色的低单价小商品。

> **⚠️ 需要坦白的取舍**: composite 的 `[R_rank, R_rank+F_rank+M_rank]` 两维**并非严格正交**, 共享 R_rank 项使得相关系数约 0.6, 相当于把 R 在距离里"算了两次" (R 权重约 F/M 的 2 倍 + 两个正交叉项)。这是**用简单性换可解释性**的自觉选择, 若被评委追问应主动承认; 追求数学严格正交的话可改用 PCA 降维, 但会牺牲"时效性/参与度"这种一眼可懂的语义。

**Q: Winsorize 是什么? 为什么要做?**
A: Winsorize 是将超过指定分位数的极端值"截断"到该分位数值。本项目截断 F/M 的 top 0.5% (即 99.5 分位以上的值)。原因: 少数极端客户 (如购买 500 次 / 消费 $20000+) 会让 K-Means 的质心被严重拉偏，截断后聚类更稳健。

**Q: 数据是怎么清洗的?**
A: 7 步流程: 去取消订单 → 去非商品编码 → 去缺失描述 → 去负数量 → 去零/负价格 → 去重复 → 去缺失客户 ID。清洗后 6 项质量指标均为 0，数据可靠 (页面 1 的清洗前后对比分组柱状图直观展示了这一点)。

**Q: 清洗后平均数量为什么反而上升 (+3.5)?**
A: 因为清洗前后的均值**不属于同一个总体**。原始 52.5 万行是"全部流水"——里面既有平均 -47 件的大额退货单 (把均值往下压)，也混着大量"游客"小额单；清洗后 39.96 万行只剩"有客户 ID 的正向销售"。按加权平均的性质，删掉一批均值远低于整体的行，剩下的均值必然上升，所以 `+3.5` 反映的是**样本构成变化**，不是数据变好或变大。被删数据的构成表与分步拆解见 **1.5 节**；同一条机制也解释了平均单价为什么反向从 $4.98 降到 $3.01。

**Q: 时间筛选器为什么只在数据探索页?**
A: RFM 和聚类分析需要客户的完整交易历史来计算 Recency (距上次购买天数)、Frequency (总购买次数) 和 Monetary (总消费金额)。截取部分时间段会破坏这些指标的计算——比如只看最后 3 个月的数据，会把很多活跃客户误判为"新客户"。数据探索页的分段统计图和热销排名则适合按时间筛选，用于观察不同时期的交易特征变化。

**Q: 为什么用分段柱状图而不是直方图? 为什么没有 3D 散点图 (其他组好像都有)?**
A: 页面 2/3 早期用学术直方图，但极端离群值会把 X 轴拉得很长、主体分布挤在最左侧看不清，而且评委更习惯"业务分段 + 占比"的读法，所以改成分段柱状图 (每段标交易行数/客户数和占比)。3D 散点图我们**做过又删掉了**，不是没实现——早期那张「客户 3D 分群可视化」把聚类结果投到原始 R/F/M 三轴上，实测在几何上不适合这个任务：**F 与 M 高度相关 (原始 Pearson 0.648，对数尺度下 log1p 相关 0.818)**，等于同一信息画了两遍；而且 F/M 极度右偏 (90% 的客户 F ≤ 9 次而轴上限 31 次，M ≤ $3,734 而轴上限 $19,734)，线性刻度下九成样本只占坐标平面约 6% 的面积，四团糊在一起。中间版本试过 99 分位截断、F/M 对数刻度，也试过换成 PC1×PC2×金额排名，量化后最好的方案也只有 0.3612 (原始 R/logF/logM 仅 0.1553)。既然 2D 图 (聚类真实空间) + 画像表 + 中心热力图已经把"簇的空间分布"讲得更准更全，再放一张必然丢信息的 3D 投影只会稀释重点，所以删除。详见 4.6 节末尾的说明。

**Q: 关联规则的支持度 2%-4.7% 是不是太低?**
A: 不低。支持度低是商品种类多的必然结果——本数据集有 4000+ 种商品，即使只取 Top 100 分析，15,446 笔交易中任意两种商品同时出现的概率天然就高不了。2% 支持度意味着约 309 笔交易同时包含这两个商品，作为统计证据已经足够。零售购物篮经典论文 (几千种 SKU) 通常用 0.5%-2% 的 min_support。真正衡量关联强度的是提升度 (lift)，我们的 top 规则 lift 达到 17.9，说明共购模式比随机出现强了近 18 倍。

---

## 代码结构说明

### 项目文件树

```
mjj_xiaozuzuoye/
├── app.py                        # Streamlit 主应用 (6 个页面, ~2100 行)
├── utils/                        # 业务逻辑模块 (纯函数, 不含 UI)
│   ├── __init__.py
│   ├── data_cleaner.py           # 数据加载与清洗
│   ├── rfm.py                    # RFM 指标计算 + 两个扩展客户特征
│   ├── clustering.py             # 特征工程 + K-Means 聚类
│   ├── association.py            # FP-Growth 关联规则挖掘 + 阈值扫描 + 营销动作
│   └── product_country.py        # 商品描述 / 用户所在国家 两个特征的业务探索
├── week1_explore.py              # 第一周数据探索脚本 (独立运行, 非大屏)
├── requirements.txt              # Python 依赖清单
├── online_retail_II(1).xlsx      # 原始数据集 (43MB, 已提交 Git 供云端读取)
├── .cache_raw.parquet            # 清洗前数据缓存 (3.4MB, 提交 Git 加速云端冷启动)
├── .cache_cleaned.parquet        # 清洗后数据缓存 (3.0MB, 同上)
├── .cache_meta.json              # Parquet 缓存指纹侧车 (须与两个缓存一起提交)
├── .gitignore                    # 忽略 .idea/ *.pptx *.docx 等
└── 大屏图表说明文档.md            # 本文档
```

### app.py — 主应用

负责所有 UI 渲染和交互逻辑，按页面分为 6 个 `if/elif` 分支。内部定义的辅助函数:

| 函数 | 装饰器 | 作用 |
|------|--------|------|
| `_cache_fingerprint(filepath)` | — | 计算 xlsx + `utils/data_cleaner.py` 的 SHA-256 内容指纹, 作为 Parquet 缓存的失效判据 |
| `load_and_clean(filepath)` | `@st.cache_data` | 加载数据: 先比对指纹侧车 `.cache_meta.json`, 一致才读 Parquet 缓存; 否则解析 xlsx 并清洗, 再回写 Parquet 与指纹。扩展特征也在这一步 merge 进 `rfm_df` |
| `filter_explore_data(_df, start, end)` | `@st.cache_data` | 按时间范围筛选数据探索页的局部副本 |
| `compute_distribution_stats(_df)` | `@st.cache_data` | 计算 Quantity/Price/Revenue 的分段统计 (labels/counts/pcts/分位数) |
| `compute_top_products(_df, top_n)` | `@st.cache_data` | 按收入聚合 Top-N 热销商品 |
| `compute_rfm_segments(_rfm_df)` | `@st.cache_data` | 计算 R/F/M 各维度的分位数和分段统计 |
| `compute_extended_segments(_rfm_df)` | `@st.cache_data` | 计算 AOV / 品类广度两个扩展特征的分段统计 (第 3 页用) |
| `cached_prepare_and_elbow(..., extra_features)` | `@st.cache_data` | 特征工程 + 肘部/轮廓系数搜索 (缓存键含特征集, 两档特征集互不干扰) |
| `cached_run_kmeans(..., extra_features)` | `@st.cache_data` | K-Means 聚类 (缓存, 相同参数秒回; 启用扩展特征时画像会多出 `Avg_AOV` / `Avg_N_products`) |
| `cached_compare_methods(..., extra_features)` | `@st.cache_data` | 在当前 K、截断参数与特征集下跑 **composite / pca 两种方法**, 返回各自的轮廓系数与特征维度, 供 4.4 expander 的实时对比表使用 (界面只暴露这两种; rank/log 实测更差、未采用) |
| `cached_association(...)` | `@st.cache_data` | FP-Growth 关联规则挖掘 + 共现矩阵 (缓存) |
| `cached_threshold_sweep(_df, top_items, _n_rows)` | `@st.cache_data` | 阈值网格扫描 (第 5 页), 返回扫描表 + 支持度上界依据 (最高频单品 / 商品对支持度) |
| `cached_description_words(_df, top_n)` | `@st.cache_data` | 商品描述高频词的品类统计 (第 6 页) |
| `cached_country_summary(_df)` | `@st.cache_data` | 按国家汇总业务表现 (第 6 页) |
| `cached_uk_overseas(_df, _rfm_df)` | `@st.cache_data` | 本土 vs 海外客户画像对比 (第 6 页) |
| `cached_country_monthly(_df, top_n)` | `@st.cache_data` | 收入 Top-N 国家的月度收入透视表 (第 6 页) |
| `_fmt_val(info, v)` | — | 根据字段类型格式化数值 (整数/美元/小数) |
| `_fmt_range(info, a, b)` | — | 格式化区间 "a 至 b" |
| `stat_cards_row(items)` | — | 渲染一行紧凑统计卡片 (HTML) |
| `_dominant_bin(info)` | — | 取分段统计中占比最高的档位 (标签, 占比%)，供第 3 页解读文案动态引用 |
| `_cn(name)` / `_cn_join(names)` | — | 商品英文名 → 中文翻译；后者用于把 `A + B` 形式的商品组合逐个翻译 (定义在第 5 页分支内) |

全局常量:
- `COLORS`: 主题色字典 (primary/secondary/success/warning/danger/info/palette); `palette` 为 15 色, 数量与侧边栏 K 上限对齐 (取色处为 `card_colors[cluster_id % len(card_colors)]`, 带取模兜底, 所以色数不足时只会复用颜色而不会报错; 对齐 15 是为了每个簇都有独立色)
- `CHART_LAYOUT`: Plotly 统一布局 (白色背景、微软雅黑字体、hoverlabel 样式)
- `QUALITY_ITEM_KEYS` / `QUALITY_EXTRA_KEYS`: 质量问题的「中文名 + `quality_report` 键名」清单 (前者 6 项供问题列表与饼图，后者只多出「缺失描述」一项)；页面 1 的三处展示共用这两份定义，避免各写一遍键名导致口径漂移
- `SUPPORT_SWEEP` / `CONFIDENCE_SWEEP`: 阈值敏感性扫描的网格刻度
- `PRODUCT_CN`: 52 个高频商品的中文翻译字典

### utils/data_cleaner.py — 数据清洗

| 函数 | 参数 | 返回 | 作用 |
|------|------|------|------|
| `load_raw_data(filepath)` | xlsx 路径 | DataFrame | 读取原始 Excel，解析日期列 |
| `clean_data(df, drop_no_customer=True)` | 原始 DataFrame | 清洗后 DataFrame | 7 步清洗流水线: 去取消订单→去非商品编码→去缺失描述→去负数量→去零/负价格→去精确重复→去缺失客户ID；添加 Revenue 列 |
| `get_data_quality_report(df)` | 任意 DataFrame | dict (总行数 + 7 类问题计数 + 各项占比%) | 逐项统计: 取消订单/缺失客户ID/负数量/零负价格/精确重复/非商品编码/缺失描述 (页面 1 的饼图与问题列表用其中 6 项；§1.6 的清洗后复检表用全部 7 项) |
| `get_cleaning_summary(raw_df, cleaned_df)` | 清洗前后 DataFrame | dict | 汇总: 原始行数/清洗后行数/移除数/移除比例/客户数/商品数/日期范围 |

### utils/rfm.py — RFM 模型与扩展特征

| 函数 | 参数 | 返回 | 作用 |
|------|------|------|------|
| `calculate_rfm(df, snapshot_date=None)` | 清洗后交易数据 | 每客户一行的 DataFrame (Customer ID, Recency, Frequency, Monetary) | 按 Customer ID 分组聚合: R=距上次购买天数, F=不重复发票数, M=总收入 |
| `add_rfm_scores(rfm_df, n_bins=5)` | RFM DataFrame | 添加 R_score/F_score/M_score/RFM_score 列 | 分位数分档 1-5 (R 反转: 越近期分越高) |
| `get_rfm_stats(rfm_df)` | RFM DataFrame | dict | 汇总统计: 客户数/均值/中位数/总收入 |
| `calculate_extended_features(df)` | 清洗后交易数据 | 每客户一行的 DataFrame (Customer ID, AOV, N_products) | 计算两个非 RFM 客户特征: **AOV = 总收入/发票数**、**N_products = 不重复商品数**。函数文档串里记录了选型依据与被否决候选的原因 |
| `EXTENDED_FEATURE_INFO` | — | dict | 两个扩展特征的中文名 / 英文名 / 计算方式 / 业务含义，供页面表格与画像列名复用，避免文案与实现两处维护 |

### utils/clustering.py — 聚类分析

| 函数 | 参数 | 返回 | 作用 |
|------|------|------|------|
| `prepare_features(rfm_df, method='composite', winsorize_pct=0.995, extra_features=None, random_state=42)` | RFM 数据, 方法名, 截断分位, **额外特征列名**, 随机种子 | (scaled_array, feature_names, scaler, transformed_df, **pca_model**) | 特征工程: Winsorize→百分位排名→StandardScaler。**模块支持四种方法**: composite / **pca** / rank / log —— **界面只用前两种** (每个特征集自动配对其最优方法; rank/log 实测更差, 保留在模块里便于复现对比)。`extra_features` (如 `['AOV','N_products']`) 以**百分位排名**追加到末尾，**不改变原有 R/F/M 处理路径**（rank 只看次序、天然抗离群，AOV 偏度 11.6 也无需额外截断）。`pca` 方法在此之上取前 2 个主成分作为聚类空间（不再二次标准化） |
| `find_optimal_k(features, k_range=range(2,11), random_state=42)` | 标准化特征 + K 搜索范围 | dict (k_values, inertia, silhouette_scores, best_k, best_silhouette, recommended_k, recommended_silhouette) | 肘部法则 + 轮廓系数搜索; recommended_k 为 K≥3 中轮廓系数最高者 |
| `run_kmeans(rfm_df, n_clusters=5, method='composite', winsorize_pct=0.995, random_state=42, extra_features=None)` | 原始 RFM + 参数 | dict (clustered_df, model, scaler, **pca**, scaled_features, silhouette_score, silhouette_per_cluster, cluster_centers, cluster_profile, method) | 完整聚类流程: 特征工程→KMeans(n_init=10, max_iter=500)→画像聚合→整体/分簇轮廓系数。传入 `extra_features` 时画像会额外聚合 `Avg_AOV` / `Avg_N_products`；`pca` 仅 PCA 方法时非 None, 供页面显示解释方差与主成分载荷 |
| `get_cluster_labels(profile)` | 聚类画像 DataFrame | dict {cluster_id: {'name': 中文名, 'traits': '近期活跃 / 高频购买 / 高消费'}} | 基于 R/F/M 均值与全体均值的比较匹配标签: 规则表 9 种业务标签 (重要价值/重要发展/新客户/流失/沉睡等) → 标签池 16 个兜底名 → 池也耗尽时生成「细分客群 N」; 三层保证侧边栏 K 上限 15 时每个簇都拿到唯一且非 None 的名称。**只依赖 R/F/M，因此新增扩展特征不会改变标签逻辑** |

### utils/association.py — 关联规则

| 函数 | 参数 | 返回 | 作用 |
|------|------|------|------|
| `prepare_basket_matrix(cleaned_df, top_n_items=150)` | 清洗后交易数据, Top-N | one-hot DataFrame (行=发票, 列=商品名, 值=bool) | 构建购物篮矩阵: 按 Invoice 分组→取 Top-N 高频商品→pd.crosstab 独热编码→列名映射为可读描述 |
| `run_fpgrowth(basket_matrix, min_support=0.02, min_confidence=0.3, min_lift=1.0)` | 购物篮矩阵 + 阈值 | dict (itemsets, rules, n_transactions, n_items) | FP-Growth 频繁项集挖掘 + association_rules 生成规则 + 按 lift 过滤排序 |
| `compute_cooccurrence_matrix(basket_matrix, top_n=20)` | 购物篮矩阵, Top-N | 方阵 DataFrame (条件概率 P(列\|行)) | 共现矩阵: sub.T @ sub 得计数→除以行对角元素得条件概率 |
| `build_network_graph(rules_df, top_n=30)` | 规则表, Top-N | dict (nodes, edges, pos) | 构建有向网络图: networkx DiGraph + spring_layout 坐标; nodes 含 id/label/degree/support, edges 含 source/target/lift/confidence |
| `threshold_sweep(basket_matrix, supports, confidences, min_lift=1.0)` | 购物篮矩阵 + 两组阈值刻度 | **(DataFrame, dict)** | 阈值敏感性扫描。利用**频繁项集对 support 单调**的性质, 只在最低支持度跑一次 fpgrowth, 再按 support 过滤 + 逐置信度生成规则, 避免几十次重复挖掘。返回的 dict 含 `max_item_support` (最高频单品支持度) 与 `max_pair_support` (最高频**商品对**支持度, 即规则支持度的天然上界) |
| `build_marketing_actions(rules_df, top_n=8)` | 规则表, Top-N | DataFrame (前项, 后项, 支持度, 置信度, 提升度, 关联力度, 建议动作) | 把规则按 lift 分档翻译成营销动作 (强≥10 / 中≥5 / 弱)。**按「前项 ∪ 后项」的商品集合去重**，避免 A→B 与 B→A 被列成两条重复方案 |

### utils/product_country.py — 商品描述与国家分析

| 函数 | 参数 | 返回 | 作用 |
|------|------|------|------|
| `description_word_stats(df, top_n=15)` | 清洗后交易数据, Top-N | DataFrame (关键词, 中文含义, 词类, 显示名, 商品数, 收入, 收入占比%, 销量, 平均单价) | 在**商品粒度**上从 Description 里提取高频词 (正则 `[A-Z][A-Z'\-]{2,}`，过滤 PACK/SET/WITH 等功能词)，把商品自动归到品类/系列，再汇总各词表现并补上中文注释。注意：一个商品会命中多个词，各词合计互相重叠 |
| `WORD_CN` | — | dict (词 → {'cn': 中文含义, 'type': 词类}) | 高频品类词的中文注释词典 (30 个词)，**逐词核对真实商品名后填写**，供第 6 页表格、图表标签与对照表展开区复用。取值统一走 `_word_cn()`，未登记的词返回「其他」占位，保证页面不出现 None |
| `country_summary(df)` | 清洗后交易数据 | DataFrame (国家, 客户数, 收入, 收入占比%, 订单数, 客单价, 复购率%, 商品种类数) | 按国家汇总。复购率 = 该国内发票数 > 1 的客户占比，用于区分"一次性买家聚集地"与"稳定复购市场" |
| `uk_vs_overseas_profile(df, rfm_df)` | 交易数据 + 客户级数据 | DataFrame (区域 × 客户数/各特征均值/收入占比%) | 按客户所属国家二分为「英国本土 / 海外」并对比画像。这是 Country 这个低方差字段最合适的用法 (它不适合直接做聚类特征) |
| `country_monthly_revenue(df, top_n=5)` | 清洗后交易数据, Top-N | DataFrame (月份 + 每个国家一列) | 收入 Top-N 国家的月度收入透视表，供堆叠面积图观察海外份额变化 |

### 模块调用关系

```
app.py
 ├── utils/data_cleaner.py    → load_raw_data / clean_data / get_data_quality_report / get_cleaning_summary
 ├── utils/rfm.py             → calculate_rfm / add_rfm_scores / get_rfm_stats / calculate_extended_features
 ├── utils/clustering.py      → prepare_features / find_optimal_k / run_kmeans / get_cluster_labels
 ├── utils/association.py     → prepare_basket_matrix / run_fpgrowth / compute_cooccurrence_matrix
 │                              / build_network_graph / threshold_sweep / build_marketing_actions
 │                                └── 内部依赖: mlxtend.frequent_patterns (fpgrowth, association_rules)
 │                                             networkx (DiGraph, spring_layout)
 └── utils/product_country.py → description_word_stats / country_summary
                                / uk_vs_overseas_profile / country_monthly_revenue
```

**数据流向 (一次完整链路)**:
```
清洗后交易数据
  ├── calculate_rfm + calculate_extended_features → rfm_df (5 个客户级维度)
  │     ├── 第 3 页: 分布、相关性矩阵、客户特征画像
  │     └── 第 4 页: prepare_features(extra_features=[...]) → K-Means → 聚类画像/标签
  ├── prepare_basket_matrix → FP-Growth → 规则 → 共现矩阵 / 网络图 / 营销方案 (第 5 页)
  └── 第 6 页: Description → 品类词统计；Country → 国家汇总 / 本土 vs 海外 (第 6 页)
```

### week1_explore.py — 第一周探索脚本

独立运行的数据探索脚本 (非大屏组件)，用于课程第一周提交。功能: 加载 xlsx→打印 shape/dtypes/head→检测缺失值→基础统计描述→识别数据质量问题。大屏开发完成后此文件仅作参考留存。

### requirements.txt — 依赖清单

```
streamlit>=1.62.0      # Web 应用框架 (下限由图表新 API width='stretch' 决定, 旧版不认字符串 width)
plotly>=5.18.0         # 交互式图表
scikit-learn>=1.4.0    # K-Means / StandardScaler / silhouette_score
pandas>=2.0.0          # 数据处理
numpy>=1.26.0          # 数值计算
openpyxl>=3.1.0        # 读取 xlsx
pyarrow>=14.0.0        # Parquet 读写
mlxtend>=0.23.0        # FP-Growth / association_rules
networkx>=3.0          # 网络图布局 (spring_layout)
```

### 模块调用关系

```
app.py
 ├── utils/data_cleaner.py    → load_raw_data / clean_data / get_data_quality_report / get_cleaning_summary
 ├── utils/rfm.py             → calculate_rfm / add_rfm_scores / get_rfm_stats
 ├── utils/clustering.py      → prepare_features / find_optimal_k / run_kmeans / get_cluster_labels
 └── utils/association.py     → prepare_basket_matrix / run_fpgrowth / compute_cooccurrence_matrix / build_network_graph
                                  └── 内部依赖: mlxtend.frequent_patterns (fpgrowth, association_rules)
                                               networkx (DiGraph, spring_layout)
```

**设计原则**: utils/ 下的模块是纯计算逻辑 (输入 DataFrame → 输出 DataFrame/dict)，不包含任何 Streamlit UI 代码。app.py 负责所有渲染、交互和缓存策略。这样便于单元测试和复用。
